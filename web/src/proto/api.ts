// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               v4.24.3
// source: api.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "sofascore";

/**
 * PushCategory is the type of event a push message represents. The
 * category drives how the Flutter client renders the notification and
 * which user-visible icon/chime to use.
 */
export enum PushCategory {
  PUSH_CATEGORY_UNSPECIFIED = 0,
  /** PUSH_CATEGORY_EVENT_ALERT - live score change, match start, etc. */
  PUSH_CATEGORY_EVENT_ALERT = 1,
  /** PUSH_CATEGORY_APK_UPDATE - new app version available */
  PUSH_CATEGORY_APK_UPDATE = 2,
  /** PUSH_CATEGORY_ADMIN_MESSAGE - free-form message from the operator */
  PUSH_CATEGORY_ADMIN_MESSAGE = 3,
  /** PUSH_CATEGORY_SCHEDULED - delivered from a scheduled_push */
  PUSH_CATEGORY_SCHEDULED = 4,
  UNRECOGNIZED = -1,
}

export function pushCategoryFromJSON(object: any): PushCategory {
  switch (object) {
    case 0:
    case "PUSH_CATEGORY_UNSPECIFIED":
      return PushCategory.PUSH_CATEGORY_UNSPECIFIED;
    case 1:
    case "PUSH_CATEGORY_EVENT_ALERT":
      return PushCategory.PUSH_CATEGORY_EVENT_ALERT;
    case 2:
    case "PUSH_CATEGORY_APK_UPDATE":
      return PushCategory.PUSH_CATEGORY_APK_UPDATE;
    case 3:
    case "PUSH_CATEGORY_ADMIN_MESSAGE":
      return PushCategory.PUSH_CATEGORY_ADMIN_MESSAGE;
    case 4:
    case "PUSH_CATEGORY_SCHEDULED":
      return PushCategory.PUSH_CATEGORY_SCHEDULED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PushCategory.UNRECOGNIZED;
  }
}

export function pushCategoryToJSON(object: PushCategory): string {
  switch (object) {
    case PushCategory.PUSH_CATEGORY_UNSPECIFIED:
      return "PUSH_CATEGORY_UNSPECIFIED";
    case PushCategory.PUSH_CATEGORY_EVENT_ALERT:
      return "PUSH_CATEGORY_EVENT_ALERT";
    case PushCategory.PUSH_CATEGORY_APK_UPDATE:
      return "PUSH_CATEGORY_APK_UPDATE";
    case PushCategory.PUSH_CATEGORY_ADMIN_MESSAGE:
      return "PUSH_CATEGORY_ADMIN_MESSAGE";
    case PushCategory.PUSH_CATEGORY_SCHEDULED:
      return "PUSH_CATEGORY_SCHEDULED";
    case PushCategory.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * PushPriority hints the client how to render the notification. HIGH may
 * bypass Do Not Disturb on capable devices.
 */
export enum PushPriority {
  PUSH_PRIORITY_UNSPECIFIED = 0,
  PUSH_PRIORITY_NORMAL = 1,
  PUSH_PRIORITY_HIGH = 2,
  UNRECOGNIZED = -1,
}

export function pushPriorityFromJSON(object: any): PushPriority {
  switch (object) {
    case 0:
    case "PUSH_PRIORITY_UNSPECIFIED":
      return PushPriority.PUSH_PRIORITY_UNSPECIFIED;
    case 1:
    case "PUSH_PRIORITY_NORMAL":
      return PushPriority.PUSH_PRIORITY_NORMAL;
    case 2:
    case "PUSH_PRIORITY_HIGH":
      return PushPriority.PUSH_PRIORITY_HIGH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PushPriority.UNRECOGNIZED;
  }
}

export function pushPriorityToJSON(object: PushPriority): string {
  switch (object) {
    case PushPriority.PUSH_PRIORITY_UNSPECIFIED:
      return "PUSH_PRIORITY_UNSPECIFIED";
    case PushPriority.PUSH_PRIORITY_NORMAL:
      return "PUSH_PRIORITY_NORMAL";
    case PushPriority.PUSH_PRIORITY_HIGH:
      return "PUSH_PRIORITY_HIGH";
    case PushPriority.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * PushScheduleType discriminates between a one_shot timestamp and a
 * recurring cron expression in ScheduledPush.
 */
export enum PushScheduleType {
  PUSH_SCHEDULE_TYPE_UNSPECIFIED = 0,
  PUSH_SCHEDULE_TYPE_ONE_SHOT = 1,
  PUSH_SCHEDULE_TYPE_RECURRING = 2,
  UNRECOGNIZED = -1,
}

export function pushScheduleTypeFromJSON(object: any): PushScheduleType {
  switch (object) {
    case 0:
    case "PUSH_SCHEDULE_TYPE_UNSPECIFIED":
      return PushScheduleType.PUSH_SCHEDULE_TYPE_UNSPECIFIED;
    case 1:
    case "PUSH_SCHEDULE_TYPE_ONE_SHOT":
      return PushScheduleType.PUSH_SCHEDULE_TYPE_ONE_SHOT;
    case 2:
    case "PUSH_SCHEDULE_TYPE_RECURRING":
      return PushScheduleType.PUSH_SCHEDULE_TYPE_RECURRING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PushScheduleType.UNRECOGNIZED;
  }
}

export function pushScheduleTypeToJSON(object: PushScheduleType): string {
  switch (object) {
    case PushScheduleType.PUSH_SCHEDULE_TYPE_UNSPECIFIED:
      return "PUSH_SCHEDULE_TYPE_UNSPECIFIED";
    case PushScheduleType.PUSH_SCHEDULE_TYPE_ONE_SHOT:
      return "PUSH_SCHEDULE_TYPE_ONE_SHOT";
    case PushScheduleType.PUSH_SCHEDULE_TYPE_RECURRING:
      return "PUSH_SCHEDULE_TYPE_RECURRING";
    case PushScheduleType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** DeliveryState is the lifecycle of a single (push, device) attempt. */
export enum DeliveryState {
  DELIVERY_STATE_UNSPECIFIED = 0,
  /** DELIVERY_STATE_SENT - WriteMessage OK, waiting for ack */
  DELIVERY_STATE_SENT = 1,
  /** DELIVERY_STATE_DELIVERED - client acked */
  DELIVERY_STATE_DELIVERED = 2,
  /** DELIVERY_STATE_FAILED - timeout, disconnect, or error */
  DELIVERY_STATE_FAILED = 3,
  UNRECOGNIZED = -1,
}

export function deliveryStateFromJSON(object: any): DeliveryState {
  switch (object) {
    case 0:
    case "DELIVERY_STATE_UNSPECIFIED":
      return DeliveryState.DELIVERY_STATE_UNSPECIFIED;
    case 1:
    case "DELIVERY_STATE_SENT":
      return DeliveryState.DELIVERY_STATE_SENT;
    case 2:
    case "DELIVERY_STATE_DELIVERED":
      return DeliveryState.DELIVERY_STATE_DELIVERED;
    case 3:
    case "DELIVERY_STATE_FAILED":
      return DeliveryState.DELIVERY_STATE_FAILED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeliveryState.UNRECOGNIZED;
  }
}

export function deliveryStateToJSON(object: DeliveryState): string {
  switch (object) {
    case DeliveryState.DELIVERY_STATE_UNSPECIFIED:
      return "DELIVERY_STATE_UNSPECIFIED";
    case DeliveryState.DELIVERY_STATE_SENT:
      return "DELIVERY_STATE_SENT";
    case DeliveryState.DELIVERY_STATE_DELIVERED:
      return "DELIVERY_STATE_DELIVERED";
    case DeliveryState.DELIVERY_STATE_FAILED:
      return "DELIVERY_STATE_FAILED";
    case DeliveryState.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * DeliveryFailureReason explains why a delivery attempt did not succeed.
 * Populated only when state == DELIVERY_STATE_FAILED.
 */
export enum DeliveryFailureReason {
  DELIVERY_FAILURE_REASON_UNSPECIFIED = 0,
  /** DELIVERY_FAILURE_REASON_DEVICE_OFFLINE - no live WS connection */
  DELIVERY_FAILURE_REASON_DEVICE_OFFLINE = 1,
  /** DELIVERY_FAILURE_REASON_SEND_TIMEOUT - ack not received in N seconds */
  DELIVERY_FAILURE_REASON_SEND_TIMEOUT = 2,
  /** DELIVERY_FAILURE_REASON_WS_DISCONNECTED - socket died mid-write */
  DELIVERY_FAILURE_REASON_WS_DISCONNECTED = 3,
  /** DELIVERY_FAILURE_REASON_DOMAIN_MISMATCH - device.domain_id != push audience */
  DELIVERY_FAILURE_REASON_DOMAIN_MISMATCH = 4,
  /** DELIVERY_FAILURE_REASON_EXPIRED_TOKEN - device token revoked */
  DELIVERY_FAILURE_REASON_EXPIRED_TOKEN = 5,
  DELIVERY_FAILURE_REASON_INTERNAL_ERROR = 6,
  UNRECOGNIZED = -1,
}

export function deliveryFailureReasonFromJSON(object: any): DeliveryFailureReason {
  switch (object) {
    case 0:
    case "DELIVERY_FAILURE_REASON_UNSPECIFIED":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_UNSPECIFIED;
    case 1:
    case "DELIVERY_FAILURE_REASON_DEVICE_OFFLINE":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_DEVICE_OFFLINE;
    case 2:
    case "DELIVERY_FAILURE_REASON_SEND_TIMEOUT":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_SEND_TIMEOUT;
    case 3:
    case "DELIVERY_FAILURE_REASON_WS_DISCONNECTED":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_WS_DISCONNECTED;
    case 4:
    case "DELIVERY_FAILURE_REASON_DOMAIN_MISMATCH":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_DOMAIN_MISMATCH;
    case 5:
    case "DELIVERY_FAILURE_REASON_EXPIRED_TOKEN":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_EXPIRED_TOKEN;
    case 6:
    case "DELIVERY_FAILURE_REASON_INTERNAL_ERROR":
      return DeliveryFailureReason.DELIVERY_FAILURE_REASON_INTERNAL_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeliveryFailureReason.UNRECOGNIZED;
  }
}

export function deliveryFailureReasonToJSON(object: DeliveryFailureReason): string {
  switch (object) {
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_UNSPECIFIED:
      return "DELIVERY_FAILURE_REASON_UNSPECIFIED";
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_DEVICE_OFFLINE:
      return "DELIVERY_FAILURE_REASON_DEVICE_OFFLINE";
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_SEND_TIMEOUT:
      return "DELIVERY_FAILURE_REASON_SEND_TIMEOUT";
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_WS_DISCONNECTED:
      return "DELIVERY_FAILURE_REASON_WS_DISCONNECTED";
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_DOMAIN_MISMATCH:
      return "DELIVERY_FAILURE_REASON_DOMAIN_MISMATCH";
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_EXPIRED_TOKEN:
      return "DELIVERY_FAILURE_REASON_EXPIRED_TOKEN";
    case DeliveryFailureReason.DELIVERY_FAILURE_REASON_INTERNAL_ERROR:
      return "DELIVERY_FAILURE_REASON_INTERNAL_ERROR";
    case DeliveryFailureReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ErrorResponse {
  error: string;
}

export interface StatusMessage {
  message: string;
}

export interface StatusResponse {
  status: string;
}

export interface AuthRequest {
  email: string;
  password: string;
  invitationToken: string;
}

export interface CreateInvitationRequest {
  ttlSeconds: number;
}

export interface InvitationResponse {
  token: string;
  expiresAt: number;
}

export interface AuthResponse {
  id: number;
  email: string;
  token: string;
  refreshToken: string;
}

export interface User {
  id: number;
  createdAt: string;
  updatedAt: string;
  email: string;
  role: string;
  /**
   * Push notifications per-user feature toggle (added 2026-08-28).
   * When false, the user cannot send pushes; the web dashboard hides the
   * push section and REST endpoints return 403.
   */
  notificationsEnabled: boolean;
  /**
   * RFC3339 timestamp of when notifications_enabled was last flipped to true;
   * empty string if the feature was never activated. Auditing only.
   */
  notificationsEnabledAt: string;
}

/**
 * SetNotificationsEnabledRequest flips the per-user push feature
 * toggle. The response is the updated User (echoed back so the
 * dashboard can refresh without a second GET).
 */
export interface SetNotificationsEnabledRequest {
  enabled: boolean;
}

export interface UserList {
  users: User[];
}

export interface UserWriteRequest {
  email: string;
  password: string;
}

export interface SetUserRoleRequest {
  role: string;
}

export interface CursorPageInfo {
  nextCursor: string;
  hasMore: boolean;
}

export interface UserPage {
  data: User[];
  page: CursorPageInfo | undefined;
}

export interface Domain {
  id: number;
  createdAt: string;
  updatedAt: string;
  domain: string;
  userId: number;
  user: User | undefined;
}

export interface DomainList {
  domains: Domain[];
}

export interface DomainPage {
  data: Domain[];
  page: CursorPageInfo | undefined;
}

export interface DomainRequest {
  domain: string;
  userId: number;
}

export interface DeviceRegisterRequest {
  token: string;
  platform: string;
  name: string;
  version: string;
  /**
   * Optional domain assignment (added 2026-08-28). When non-zero, the
   * backend sets devices.domain_id to this value (validating that the
   * domain exists; per-user ownership is not enforced at register time
   * because the Flutter app does not authenticate as a user).
   */
  domainId: number;
}

export interface Device {
  id: number;
  createdAt: string;
  updatedAt: string;
  token: string;
  platform: string;
  name: string;
  lastSeen: number;
  version: string;
  iptvUrl: string;
  /**
   * Domain the device is associated with (added 2026-08-28). Drives the
   * push audience filter: a push to domain X only reaches devices whose
   * domain_id == X. 0 means "unassigned"; such devices are excluded from
   * push delivery.
   */
  domainId: number;
}

export interface DeviceList {
  data: Device[];
  page: number;
  limit: number;
  total: number;
  totalPages: number;
}

export interface DevicePage {
  data: Device[];
  page: CursorPageInfo | undefined;
}

export interface DeviceUrl {
  url: string;
}

export interface TournamentRequest {
  name: string;
  slug: string;
}

export interface Tournament {
  id: number;
  createdAt: string;
  updatedAt: string;
  name: string;
  slug: string;
  region: string;
}

export interface TournamentList {
  tournaments: Tournament[];
}

export interface TournamentPage {
  data: Tournament[];
  page: CursorPageInfo | undefined;
}

export interface AssignTournamentRequest {
  deviceId: number;
  tournamentId: number;
}

export interface SetTournamentIdsRequest {
  tournamentIds: number[];
}

export interface DeviceTournament {
  id: number;
  createdAt: string;
  updatedAt: string;
  deviceId: number;
  tournamentId: number;
  device: Device | undefined;
  tournament: Tournament | undefined;
}

export interface DeviceTournamentList {
  deviceTournaments: DeviceTournament[];
}

export interface DeviceTournamentPage {
  data: DeviceTournament[];
  page: CursorPageInfo | undefined;
}

export interface GlobalTournamentConfig {
  id: number;
  createdAt: string;
  updatedAt: string;
  tournamentId: number;
  tournament: Tournament | undefined;
}

export interface GlobalTournamentConfigList {
  configs: GlobalTournamentConfig[];
}

export interface Team {
  id: number;
  teamId: number;
  logoUrl: string;
  primaryColor: string;
  secondaryColor: string;
  textColor: string;
  name: string;
}

export interface SofaScoreEvent {
  id: number;
  createdAt: string;
  updatedAt: string;
  sofaScoreEventId: number;
  sport: string;
  homeScore: number;
  homeTeamId: number;
  awayScore: number;
  awayTeamId: number;
  scrapedAt: number;
  category: string;
  startTimestamp: number;
  currentPeriodStartTimestamp: number;
  slug: string;
  teamHome: Team | undefined;
  teamAway: Team | undefined;
  league: Tournament | undefined;
  statusType: string;
}

export interface EventsList {
  data: SofaScoreEvent[];
  page: number;
  limit: number;
  total: number;
  totalPages: number;
}

export interface EventPage {
  data: SofaScoreEvent[];
  page: CursorPageInfo | undefined;
}

export interface LogPlaybackRequest {
  deviceToken: string;
  content: string;
  startedAt: number;
}

export interface UpdatePlaybackRequest {
  endedAt: number;
}

export interface PlaybackLog {
  id: number;
  createdAt: string;
  updatedAt: string;
  deviceId: number;
  content: string;
  startedAt: number;
  endedAt: number;
}

export interface PlaybackLogList {
  list: PlaybackLog[];
  total: number;
}

export interface PlaybackPage {
  data: PlaybackLog[];
  page: CursorPageInfo | undefined;
}

export interface EventStats {
  sofaScoreEventId: number;
  viewCount: number;
}

export interface TopEventsResponse {
  stats: EventStats[];
}

export interface ApkInfo {
  id: number;
  version: string;
  fileName: string;
  fileSize: number;
  description: string;
  packageName: string;
  versionCode: number;
  minSdkVersion: number;
  targetSdkVersion: number;
  downloadToken: string;
  downloadUrl: string;
  createdAt: string;
  isActive: boolean;
  downloads: number;
  panelUrl: string;
}

export interface ApkList {
  versions: ApkInfo[];
}

export interface ApkPage {
  data: ApkInfo[];
  page: CursorPageInfo | undefined;
}

export interface ApkUploadResponse {
  id: number;
  version: string;
  fileName: string;
  fileSize: number;
  description: string;
  packageName: string;
  versionCode: number;
  minSdkVersion: number;
  targetSdkVersion: number;
  downloadToken: string;
  downloadUrl: string;
  createdAt: string;
}

export interface ApkUpdateCheckResponse {
  updateAvailable: boolean;
  latestVersion: string;
  packageName: string;
  versionCode: number;
  downloadUrl: string;
  description: string;
  fileSize: number;
  minSdkVersion: number;
  targetSdkVersion: number;
}

export interface ApkVersion {
  id: number;
  url: string;
}

export interface UploadBeginRequest {
  fileName: string;
  fileSize: number;
  totalChunks: number;
  version: string;
  description: string;
}

export interface UploadBeginResponse {
  uploadId: string;
  maxChunkSize: number;
  maxTotalChunks: number;
  maxAggregateSize: number;
  expiresAt: number;
  status: string;
  chunksReceived: number;
  bytesReceived: number;
}

export interface UploadStatusResponse {
  uploadId: string;
  status: string;
  totalChunks: number;
  chunksReceived: number;
  bytesReceived: number;
  expiresAt: number;
  fileName: string;
  fileSize: number;
}

export interface UploadChunkResponse {
  uploadId: string;
  chunkIndex: number;
  ok: boolean;
}

export interface UploadCompleteResponse {
  id: number;
  version: string;
  fileName: string;
  fileSize: number;
  description: string;
  packageName: string;
  versionCode: number;
  minSdkVersion: number;
  targetSdkVersion: number;
  downloadToken: string;
  downloadUrl: string;
  createdAt: string;
}

/**
 * PushPayload is the user-authored content carried by both immediate and
 * scheduled pushes. All fields are stored denormalized on push_messages
 * (and scheduled_pushes) so we never need to join at delivery time.
 */
export interface PushPayload {
  category: PushCategory;
  title: string;
  body: string;
  imageUrl: string;
  deepLink: string;
  priority: PushPriority;
  /** 0 = no TTL */
  ttlSeconds: number;
  /** free-form metadata */
  data: { [key: string]: string };
}

export interface PushPayload_DataEntry {
  key: string;
  value: string;
}

export interface CreateImmediatePushRequest {
  /** one or many, all must belong to caller */
  domainIds: number[];
  payload: PushPayload | undefined;
}

export interface CreateScheduleRequest {
  domainIds: number[];
  payload: PushPayload | undefined;
  scheduleType: PushScheduleType;
  /** RFC3339; required when schedule_type == ONE_SHOT */
  runAt: string;
  /** 5-field cron; required when schedule_type == RECURRING */
  cronExpr: string;
}

/**
 * UpdateScheduleRequest intentionally omits cron_expr and run_at. Those
 * fields are immutable after create; if the operator needs to change the
 * schedule, the existing schedule is deleted and a new one is created.
 * This avoids races with the scheduler runner that already calculated
 * next_fire_at from the old expression.
 */
export interface UpdateScheduleRequest {
  id: number;
  isActive: boolean;
  payload: PushPayload | undefined;
}

export interface ScheduledPush {
  id: number;
  createdAt: string;
  updatedAt: string;
  userId: number;
  scheduleType: PushScheduleType;
  /** populated for one_shot */
  runAt: string;
  /** populated for recurring */
  cronExpr: string;
  nextFireAt: string;
  /** empty if never fired */
  lastFiredAt: string;
  isActive: boolean;
  domainIds: number[];
  payload: PushPayload | undefined;
}

export interface ScheduledPushPage {
  data: ScheduledPush[];
  page: CursorPageInfo | undefined;
}

export interface PushMessage {
  id: number;
  createdAt: string;
  userId: number;
  category: PushCategory;
  title: string;
  body: string;
  imageUrl: string;
  deepLink: string;
  priority: PushPriority;
  ttlSeconds: number;
  data: { [key: string]: string };
  /** "immediate" | "scheduled" */
  source: string;
  /** 0 when source == "immediate" */
  scheduledId: number;
  domainIds: number[];
}

export interface PushMessage_DataEntry {
  key: string;
  value: string;
}

export interface PushMessagePage {
  data: PushMessage[];
  page: CursorPageInfo | undefined;
}

/** Per-campaign metrics returned by GET /pushes/{id}. */
export interface FailureBreakdown {
  reason: DeliveryFailureReason;
  count: number;
}

export interface PushMetricsByCampaign {
  pushId: number;
  targetsTotal: number;
  delivered: number;
  notDelivered: number;
  /** delivered / targets_total */
  deliveryRate: number;
  latencyP50Ms: number;
  latencyP95Ms: number;
  failuresByReason: FailureBreakdown[];
}

/** Aggregated metrics returned by GET /pushes/metrics/aggregate. */
export interface PlatformCount {
  platform: string;
  count: number;
}

export interface AppVersionCount {
  version: string;
  count: number;
}

export interface HourBucket {
  hour: number;
  count: number;
}

export interface PushMetricsAggregate {
  messagesSentTotal: number;
  messagesDeliveredTotal: number;
  deliveryRateTotal: number;
  activeSchedules: number;
  scheduledFires24h: number;
  scheduledFires7d: number;
  scheduledFires30d: number;
  scheduledFailures24h: number;
  scheduledFailures7d: number;
  scheduledFailures30d: number;
  /** devices currently connected via WS */
  audienceSize: number;
  audiencePeakToday: number;
  topPlatforms: PlatformCount[];
  topAppVersions: AppVersionCount[];
  hourlyHistogram30d: HourBucket[];
  /** RFC3339; empty if never */
  lastPushAt: string;
}

/**
 * ========== WebSocket frames (added 2026-08-28) ==========
 *
 * WsFrame is the single envelope used on the WebSocket connection. Every
 * frame is a proto-encoded WsFrame; the receiver dispatches based on
 * which oneof case is set. This keeps the wire format uniform and lets
 * the client/server stay forward-compatible with new frame types.
 */
export interface WsFrame {
  /** server -> client on connect */
  hello?:
    | WsHello
    | undefined;
  /** server -> client: a push delivery */
  push?:
    | WsPush
    | undefined;
  /** client -> server: confirms a push */
  pushAck?:
    | WsPushAck
    | undefined;
  /** bidirectional keepalive */
  ping?: WsPing | undefined;
  pong?:
    | WsPong
    | undefined;
  /** server -> client: terminal error before close */
  error?: WsError | undefined;
}

export interface WsHello {
  deviceId: number;
  /** unix seconds */
  serverTime: number;
}

export interface WsPush {
  /** = push_messages.id */
  pushId: number;
  /** unique per attempt; client must echo in ack */
  messageId: number;
  category: PushCategory;
  title: string;
  body: string;
  imageUrl: string;
  deepLink: string;
  priority: PushPriority;
  ttlSeconds: number;
  data: { [key: string]: string };
  /** unix milliseconds; client uses for latency */
  sentAt: number;
}

export interface WsPush_DataEntry {
  key: string;
  value: string;
}

export interface WsPushAck {
  /** identifies the push_messages row; the server uses it to look up delivery_attempts */
  pushId: number;
  /** echoes WsPush.message_id (kept for client-side latency display) */
  messageId: number;
  /** unix milliseconds */
  ackedAt: number;
}

export interface WsPing {
  sentAt: number;
}

export interface WsPong {
  sentAt: number;
}

export interface WsError {
  /** invalid_token | domain_mismatch | rate_limited | internal */
  code: string;
  message: string;
}

function createBaseErrorResponse(): ErrorResponse {
  return { error: "" };
}

export const ErrorResponse: MessageFns<ErrorResponse> = {
  encode(message: ErrorResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== "") {
      writer.uint32(10).string(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.error = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ErrorResponse {
    return { error: isSet(object.error) ? globalThis.String(object.error) : "" };
  },

  toJSON(message: ErrorResponse): unknown {
    const obj: any = {};
    if (message.error !== "") {
      obj.error = message.error;
    }
    return obj;
  },

  create(base?: DeepPartial<ErrorResponse>): ErrorResponse {
    return ErrorResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ErrorResponse>): ErrorResponse {
    const message = createBaseErrorResponse();
    message.error = object.error ?? "";
    return message;
  },
};

function createBaseStatusMessage(): StatusMessage {
  return { message: "" };
}

export const StatusMessage: MessageFns<StatusMessage> = {
  encode(message: StatusMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusMessage {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: StatusMessage): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<StatusMessage>): StatusMessage {
    return StatusMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatusMessage>): StatusMessage {
    const message = createBaseStatusMessage();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseStatusResponse(): StatusResponse {
  return { status: "" };
}

export const StatusResponse: MessageFns<StatusResponse> = {
  encode(message: StatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== "") {
      writer.uint32(10).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusResponse {
    return { status: isSet(object.status) ? globalThis.String(object.status) : "" };
  },

  toJSON(message: StatusResponse): unknown {
    const obj: any = {};
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create(base?: DeepPartial<StatusResponse>): StatusResponse {
    return StatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StatusResponse>): StatusResponse {
    const message = createBaseStatusResponse();
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseAuthRequest(): AuthRequest {
  return { email: "", password: "", invitationToken: "" };
}

export const AuthRequest: MessageFns<AuthRequest> = {
  encode(message: AuthRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    if (message.invitationToken !== "") {
      writer.uint32(26).string(message.invitationToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.invitationToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
      invitationToken: isSet(object.invitationToken)
        ? globalThis.String(object.invitationToken)
        : isSet(object.invitation_token)
        ? globalThis.String(object.invitation_token)
        : "",
    };
  },

  toJSON(message: AuthRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    if (message.invitationToken !== "") {
      obj.invitationToken = message.invitationToken;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthRequest>): AuthRequest {
    return AuthRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthRequest>): AuthRequest {
    const message = createBaseAuthRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    message.invitationToken = object.invitationToken ?? "";
    return message;
  },
};

function createBaseCreateInvitationRequest(): CreateInvitationRequest {
  return { ttlSeconds: 0 };
}

export const CreateInvitationRequest: MessageFns<CreateInvitationRequest> = {
  encode(message: CreateInvitationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ttlSeconds !== 0) {
      writer.uint32(8).int64(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateInvitationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateInvitationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.ttlSeconds = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateInvitationRequest {
    return {
      ttlSeconds: isSet(object.ttlSeconds)
        ? globalThis.Number(object.ttlSeconds)
        : isSet(object.ttl_seconds)
        ? globalThis.Number(object.ttl_seconds)
        : 0,
    };
  },

  toJSON(message: CreateInvitationRequest): unknown {
    const obj: any = {};
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateInvitationRequest>): CreateInvitationRequest {
    return CreateInvitationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateInvitationRequest>): CreateInvitationRequest {
    const message = createBaseCreateInvitationRequest();
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseInvitationResponse(): InvitationResponse {
  return { token: "", expiresAt: 0 };
}

export const InvitationResponse: MessageFns<InvitationResponse> = {
  encode(message: InvitationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.expiresAt !== 0) {
      writer.uint32(16).int64(message.expiresAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InvitationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInvitationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.expiresAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InvitationResponse {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      expiresAt: isSet(object.expiresAt)
        ? globalThis.Number(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.Number(object.expires_at)
        : 0,
    };
  },

  toJSON(message: InvitationResponse): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    return obj;
  },

  create(base?: DeepPartial<InvitationResponse>): InvitationResponse {
    return InvitationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<InvitationResponse>): InvitationResponse {
    const message = createBaseInvitationResponse();
    message.token = object.token ?? "";
    message.expiresAt = object.expiresAt ?? 0;
    return message;
  },
};

function createBaseAuthResponse(): AuthResponse {
  return { id: 0, email: "", token: "", refreshToken: "" };
}

export const AuthResponse: MessageFns<AuthResponse> = {
  encode(message: AuthResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.email !== "") {
      writer.uint32(18).string(message.email);
    }
    if (message.token !== "") {
      writer.uint32(26).string(message.token);
    }
    if (message.refreshToken !== "") {
      writer.uint32(34).string(message.refreshToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.refreshToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthResponse {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      refreshToken: isSet(object.refreshToken)
        ? globalThis.String(object.refreshToken)
        : isSet(object.refresh_token)
        ? globalThis.String(object.refresh_token)
        : "",
    };
  },

  toJSON(message: AuthResponse): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.refreshToken !== "") {
      obj.refreshToken = message.refreshToken;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthResponse>): AuthResponse {
    return AuthResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthResponse>): AuthResponse {
    const message = createBaseAuthResponse();
    message.id = object.id ?? 0;
    message.email = object.email ?? "";
    message.token = object.token ?? "";
    message.refreshToken = object.refreshToken ?? "";
    return message;
  },
};

function createBaseUser(): User {
  return {
    id: 0,
    createdAt: "",
    updatedAt: "",
    email: "",
    role: "",
    notificationsEnabled: false,
    notificationsEnabledAt: "",
  };
}

export const User: MessageFns<User> = {
  encode(message: User, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.email !== "") {
      writer.uint32(34).string(message.email);
    }
    if (message.role !== "") {
      writer.uint32(42).string(message.role);
    }
    if (message.notificationsEnabled !== false) {
      writer.uint32(48).bool(message.notificationsEnabled);
    }
    if (message.notificationsEnabledAt !== "") {
      writer.uint32(58).string(message.notificationsEnabledAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): User {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUser();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.role = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.notificationsEnabled = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.notificationsEnabledAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): User {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      role: isSet(object.role) ? globalThis.String(object.role) : "",
      notificationsEnabled: isSet(object.notificationsEnabled)
        ? globalThis.Boolean(object.notificationsEnabled)
        : isSet(object.notifications_enabled)
        ? globalThis.Boolean(object.notifications_enabled)
        : false,
      notificationsEnabledAt: isSet(object.notificationsEnabledAt)
        ? globalThis.String(object.notificationsEnabledAt)
        : isSet(object.notifications_enabled_at)
        ? globalThis.String(object.notifications_enabled_at)
        : "",
    };
  },

  toJSON(message: User): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.role !== "") {
      obj.role = message.role;
    }
    if (message.notificationsEnabled !== false) {
      obj.notificationsEnabled = message.notificationsEnabled;
    }
    if (message.notificationsEnabledAt !== "") {
      obj.notificationsEnabledAt = message.notificationsEnabledAt;
    }
    return obj;
  },

  create(base?: DeepPartial<User>): User {
    return User.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<User>): User {
    const message = createBaseUser();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.email = object.email ?? "";
    message.role = object.role ?? "";
    message.notificationsEnabled = object.notificationsEnabled ?? false;
    message.notificationsEnabledAt = object.notificationsEnabledAt ?? "";
    return message;
  },
};

function createBaseSetNotificationsEnabledRequest(): SetNotificationsEnabledRequest {
  return { enabled: false };
}

export const SetNotificationsEnabledRequest: MessageFns<SetNotificationsEnabledRequest> = {
  encode(message: SetNotificationsEnabledRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.enabled !== false) {
      writer.uint32(8).bool(message.enabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetNotificationsEnabledRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetNotificationsEnabledRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetNotificationsEnabledRequest {
    return { enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false };
  },

  toJSON(message: SetNotificationsEnabledRequest): unknown {
    const obj: any = {};
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    return obj;
  },

  create(base?: DeepPartial<SetNotificationsEnabledRequest>): SetNotificationsEnabledRequest {
    return SetNotificationsEnabledRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetNotificationsEnabledRequest>): SetNotificationsEnabledRequest {
    const message = createBaseSetNotificationsEnabledRequest();
    message.enabled = object.enabled ?? false;
    return message;
  },
};

function createBaseUserList(): UserList {
  return { users: [] };
}

export const UserList: MessageFns<UserList> = {
  encode(message: UserList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.users) {
      User.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.users.push(User.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserList {
    return { users: globalThis.Array.isArray(object?.users) ? object.users.map((e: any) => User.fromJSON(e)) : [] };
  },

  toJSON(message: UserList): unknown {
    const obj: any = {};
    if (message.users?.length) {
      obj.users = message.users.map((e) => User.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<UserList>): UserList {
    return UserList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserList>): UserList {
    const message = createBaseUserList();
    message.users = object.users?.map((e) => User.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUserWriteRequest(): UserWriteRequest {
  return { email: "", password: "" };
}

export const UserWriteRequest: MessageFns<UserWriteRequest> = {
  encode(message: UserWriteRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserWriteRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserWriteRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserWriteRequest {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: UserWriteRequest): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create(base?: DeepPartial<UserWriteRequest>): UserWriteRequest {
    return UserWriteRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserWriteRequest>): UserWriteRequest {
    const message = createBaseUserWriteRequest();
    message.email = object.email ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseSetUserRoleRequest(): SetUserRoleRequest {
  return { role: "" };
}

export const SetUserRoleRequest: MessageFns<SetUserRoleRequest> = {
  encode(message: SetUserRoleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.role !== "") {
      writer.uint32(10).string(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetUserRoleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetUserRoleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.role = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetUserRoleRequest {
    return { role: isSet(object.role) ? globalThis.String(object.role) : "" };
  },

  toJSON(message: SetUserRoleRequest): unknown {
    const obj: any = {};
    if (message.role !== "") {
      obj.role = message.role;
    }
    return obj;
  },

  create(base?: DeepPartial<SetUserRoleRequest>): SetUserRoleRequest {
    return SetUserRoleRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetUserRoleRequest>): SetUserRoleRequest {
    const message = createBaseSetUserRoleRequest();
    message.role = object.role ?? "";
    return message;
  },
};

function createBaseCursorPageInfo(): CursorPageInfo {
  return { nextCursor: "", hasMore: false };
}

export const CursorPageInfo: MessageFns<CursorPageInfo> = {
  encode(message: CursorPageInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nextCursor !== "") {
      writer.uint32(10).string(message.nextCursor);
    }
    if (message.hasMore !== false) {
      writer.uint32(16).bool(message.hasMore);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CursorPageInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCursorPageInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.nextCursor = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.hasMore = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CursorPageInfo {
    return {
      nextCursor: isSet(object.nextCursor)
        ? globalThis.String(object.nextCursor)
        : isSet(object.next_cursor)
        ? globalThis.String(object.next_cursor)
        : "",
      hasMore: isSet(object.hasMore)
        ? globalThis.Boolean(object.hasMore)
        : isSet(object.has_more)
        ? globalThis.Boolean(object.has_more)
        : false,
    };
  },

  toJSON(message: CursorPageInfo): unknown {
    const obj: any = {};
    if (message.nextCursor !== "") {
      obj.nextCursor = message.nextCursor;
    }
    if (message.hasMore !== false) {
      obj.hasMore = message.hasMore;
    }
    return obj;
  },

  create(base?: DeepPartial<CursorPageInfo>): CursorPageInfo {
    return CursorPageInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CursorPageInfo>): CursorPageInfo {
    const message = createBaseCursorPageInfo();
    message.nextCursor = object.nextCursor ?? "";
    message.hasMore = object.hasMore ?? false;
    return message;
  },
};

function createBaseUserPage(): UserPage {
  return { data: [], page: undefined };
}

export const UserPage: MessageFns<UserPage> = {
  encode(message: UserPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      User.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(User.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => User.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: UserPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => User.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<UserPage>): UserPage {
    return UserPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UserPage>): UserPage {
    const message = createBaseUserPage();
    message.data = object.data?.map((e) => User.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseDomain(): Domain {
  return { id: 0, createdAt: "", updatedAt: "", domain: "", userId: 0, user: undefined };
}

export const Domain: MessageFns<Domain> = {
  encode(message: Domain, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.domain !== "") {
      writer.uint32(34).string(message.domain);
    }
    if (message.userId !== 0) {
      writer.uint32(40).uint32(message.userId);
    }
    if (message.user !== undefined) {
      User.encode(message.user, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Domain {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDomain();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.userId = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.user = User.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Domain {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      user: isSet(object.user) ? User.fromJSON(object.user) : undefined,
    };
  },

  toJSON(message: Domain): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.user !== undefined) {
      obj.user = User.toJSON(message.user);
    }
    return obj;
  },

  create(base?: DeepPartial<Domain>): Domain {
    return Domain.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Domain>): Domain {
    const message = createBaseDomain();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.domain = object.domain ?? "";
    message.userId = object.userId ?? 0;
    message.user = (object.user !== undefined && object.user !== null) ? User.fromPartial(object.user) : undefined;
    return message;
  },
};

function createBaseDomainList(): DomainList {
  return { domains: [] };
}

export const DomainList: MessageFns<DomainList> = {
  encode(message: DomainList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.domains) {
      Domain.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DomainList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDomainList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domains.push(Domain.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DomainList {
    return {
      domains: globalThis.Array.isArray(object?.domains) ? object.domains.map((e: any) => Domain.fromJSON(e)) : [],
    };
  },

  toJSON(message: DomainList): unknown {
    const obj: any = {};
    if (message.domains?.length) {
      obj.domains = message.domains.map((e) => Domain.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DomainList>): DomainList {
    return DomainList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DomainList>): DomainList {
    const message = createBaseDomainList();
    message.domains = object.domains?.map((e) => Domain.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDomainPage(): DomainPage {
  return { data: [], page: undefined };
}

export const DomainPage: MessageFns<DomainPage> = {
  encode(message: DomainPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      Domain.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DomainPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDomainPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(Domain.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DomainPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => Domain.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: DomainPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => Domain.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<DomainPage>): DomainPage {
    return DomainPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DomainPage>): DomainPage {
    const message = createBaseDomainPage();
    message.data = object.data?.map((e) => Domain.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseDomainRequest(): DomainRequest {
  return { domain: "", userId: 0 };
}

export const DomainRequest: MessageFns<DomainRequest> = {
  encode(message: DomainRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.userId !== 0) {
      writer.uint32(16).uint32(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DomainRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDomainRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.userId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DomainRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
    };
  },

  toJSON(message: DomainRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create(base?: DeepPartial<DomainRequest>): DomainRequest {
    return DomainRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DomainRequest>): DomainRequest {
    const message = createBaseDomainRequest();
    message.domain = object.domain ?? "";
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseDeviceRegisterRequest(): DeviceRegisterRequest {
  return { token: "", platform: "", name: "", version: "", domainId: 0 };
}

export const DeviceRegisterRequest: MessageFns<DeviceRegisterRequest> = {
  encode(message: DeviceRegisterRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    if (message.platform !== "") {
      writer.uint32(18).string(message.platform);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.version !== "") {
      writer.uint32(34).string(message.version);
    }
    if (message.domainId !== 0) {
      writer.uint32(40).uint32(message.domainId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceRegisterRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceRegisterRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.domainId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceRegisterRequest {
    return {
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      domainId: isSet(object.domainId)
        ? globalThis.Number(object.domainId)
        : isSet(object.domain_id)
        ? globalThis.Number(object.domain_id)
        : 0,
    };
  },

  toJSON(message: DeviceRegisterRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.domainId !== 0) {
      obj.domainId = Math.round(message.domainId);
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceRegisterRequest>): DeviceRegisterRequest {
    return DeviceRegisterRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceRegisterRequest>): DeviceRegisterRequest {
    const message = createBaseDeviceRegisterRequest();
    message.token = object.token ?? "";
    message.platform = object.platform ?? "";
    message.name = object.name ?? "";
    message.version = object.version ?? "";
    message.domainId = object.domainId ?? 0;
    return message;
  },
};

function createBaseDevice(): Device {
  return {
    id: 0,
    createdAt: "",
    updatedAt: "",
    token: "",
    platform: "",
    name: "",
    lastSeen: 0,
    version: "",
    iptvUrl: "",
    domainId: 0,
  };
}

export const Device: MessageFns<Device> = {
  encode(message: Device, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.token !== "") {
      writer.uint32(34).string(message.token);
    }
    if (message.platform !== "") {
      writer.uint32(42).string(message.platform);
    }
    if (message.name !== "") {
      writer.uint32(50).string(message.name);
    }
    if (message.lastSeen !== 0) {
      writer.uint32(56).int64(message.lastSeen);
    }
    if (message.version !== "") {
      writer.uint32(66).string(message.version);
    }
    if (message.iptvUrl !== "") {
      writer.uint32(74).string(message.iptvUrl);
    }
    if (message.domainId !== 0) {
      writer.uint32(80).uint32(message.domainId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Device {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.token = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.lastSeen = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.iptvUrl = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.domainId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Device {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      token: isSet(object.token) ? globalThis.String(object.token) : "",
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      lastSeen: isSet(object.lastSeen)
        ? globalThis.Number(object.lastSeen)
        : isSet(object.last_seen)
        ? globalThis.Number(object.last_seen)
        : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      iptvUrl: isSet(object.iptvUrl)
        ? globalThis.String(object.iptvUrl)
        : isSet(object.iptv_url)
        ? globalThis.String(object.iptv_url)
        : "",
      domainId: isSet(object.domainId)
        ? globalThis.Number(object.domainId)
        : isSet(object.domain_id)
        ? globalThis.Number(object.domain_id)
        : 0,
    };
  },

  toJSON(message: Device): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.token !== "") {
      obj.token = message.token;
    }
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.lastSeen !== 0) {
      obj.lastSeen = Math.round(message.lastSeen);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.iptvUrl !== "") {
      obj.iptvUrl = message.iptvUrl;
    }
    if (message.domainId !== 0) {
      obj.domainId = Math.round(message.domainId);
    }
    return obj;
  },

  create(base?: DeepPartial<Device>): Device {
    return Device.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Device>): Device {
    const message = createBaseDevice();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.token = object.token ?? "";
    message.platform = object.platform ?? "";
    message.name = object.name ?? "";
    message.lastSeen = object.lastSeen ?? 0;
    message.version = object.version ?? "";
    message.iptvUrl = object.iptvUrl ?? "";
    message.domainId = object.domainId ?? 0;
    return message;
  },
};

function createBaseDeviceList(): DeviceList {
  return { data: [], page: 0, limit: 0, total: 0, totalPages: 0 };
}

export const DeviceList: MessageFns<DeviceList> = {
  encode(message: DeviceList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      Device.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== 0) {
      writer.uint32(16).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.total !== 0) {
      writer.uint32(32).int64(message.total);
    }
    if (message.totalPages !== 0) {
      writer.uint32(40).int32(message.totalPages);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(Device.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.total = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalPages = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceList {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => Device.fromJSON(e)) : [],
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      totalPages: isSet(object.totalPages)
        ? globalThis.Number(object.totalPages)
        : isSet(object.total_pages)
        ? globalThis.Number(object.total_pages)
        : 0,
    };
  },

  toJSON(message: DeviceList): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => Device.toJSON(e));
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.totalPages !== 0) {
      obj.totalPages = Math.round(message.totalPages);
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceList>): DeviceList {
    return DeviceList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceList>): DeviceList {
    const message = createBaseDeviceList();
    message.data = object.data?.map((e) => Device.fromPartial(e)) || [];
    message.page = object.page ?? 0;
    message.limit = object.limit ?? 0;
    message.total = object.total ?? 0;
    message.totalPages = object.totalPages ?? 0;
    return message;
  },
};

function createBaseDevicePage(): DevicePage {
  return { data: [], page: undefined };
}

export const DevicePage: MessageFns<DevicePage> = {
  encode(message: DevicePage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      Device.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DevicePage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevicePage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(Device.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DevicePage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => Device.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: DevicePage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => Device.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<DevicePage>): DevicePage {
    return DevicePage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DevicePage>): DevicePage {
    const message = createBaseDevicePage();
    message.data = object.data?.map((e) => Device.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseDeviceUrl(): DeviceUrl {
  return { url: "" };
}

export const DeviceUrl: MessageFns<DeviceUrl> = {
  encode(message: DeviceUrl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceUrl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceUrl();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceUrl {
    return { url: isSet(object.url) ? globalThis.String(object.url) : "" };
  },

  toJSON(message: DeviceUrl): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceUrl>): DeviceUrl {
    return DeviceUrl.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceUrl>): DeviceUrl {
    const message = createBaseDeviceUrl();
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseTournamentRequest(): TournamentRequest {
  return { name: "", slug: "" };
}

export const TournamentRequest: MessageFns<TournamentRequest> = {
  encode(message: TournamentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.slug !== "") {
      writer.uint32(18).string(message.slug);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
    };
  },

  toJSON(message: TournamentRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentRequest>): TournamentRequest {
    return TournamentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentRequest>): TournamentRequest {
    const message = createBaseTournamentRequest();
    message.name = object.name ?? "";
    message.slug = object.slug ?? "";
    return message;
  },
};

function createBaseTournament(): Tournament {
  return { id: 0, createdAt: "", updatedAt: "", name: "", slug: "", region: "" };
}

export const Tournament: MessageFns<Tournament> = {
  encode(message: Tournament, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.name !== "") {
      writer.uint32(34).string(message.name);
    }
    if (message.slug !== "") {
      writer.uint32(42).string(message.slug);
    }
    if (message.region !== "") {
      writer.uint32(50).string(message.region);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tournament {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournament();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.region = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tournament {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
    };
  },

  toJSON(message: Tournament): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    return obj;
  },

  create(base?: DeepPartial<Tournament>): Tournament {
    return Tournament.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Tournament>): Tournament {
    const message = createBaseTournament();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.name = object.name ?? "";
    message.slug = object.slug ?? "";
    message.region = object.region ?? "";
    return message;
  },
};

function createBaseTournamentList(): TournamentList {
  return { tournaments: [] };
}

export const TournamentList: MessageFns<TournamentList> = {
  encode(message: TournamentList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tournaments) {
      Tournament.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tournaments.push(Tournament.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentList {
    return {
      tournaments: globalThis.Array.isArray(object?.tournaments)
        ? object.tournaments.map((e: any) => Tournament.fromJSON(e))
        : [],
    };
  },

  toJSON(message: TournamentList): unknown {
    const obj: any = {};
    if (message.tournaments?.length) {
      obj.tournaments = message.tournaments.map((e) => Tournament.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentList>): TournamentList {
    return TournamentList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentList>): TournamentList {
    const message = createBaseTournamentList();
    message.tournaments = object.tournaments?.map((e) => Tournament.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTournamentPage(): TournamentPage {
  return { data: [], page: undefined };
}

export const TournamentPage: MessageFns<TournamentPage> = {
  encode(message: TournamentPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      Tournament.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TournamentPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTournamentPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(Tournament.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TournamentPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => Tournament.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: TournamentPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => Tournament.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<TournamentPage>): TournamentPage {
    return TournamentPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TournamentPage>): TournamentPage {
    const message = createBaseTournamentPage();
    message.data = object.data?.map((e) => Tournament.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseAssignTournamentRequest(): AssignTournamentRequest {
  return { deviceId: 0, tournamentId: 0 };
}

export const AssignTournamentRequest: MessageFns<AssignTournamentRequest> = {
  encode(message: AssignTournamentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== 0) {
      writer.uint32(8).uint32(message.deviceId);
    }
    if (message.tournamentId !== 0) {
      writer.uint32(16).uint32(message.tournamentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AssignTournamentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAssignTournamentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deviceId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.tournamentId = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AssignTournamentRequest {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
      tournamentId: isSet(object.tournamentId)
        ? globalThis.Number(object.tournamentId)
        : isSet(object.tournament_id)
        ? globalThis.Number(object.tournament_id)
        : 0,
    };
  },

  toJSON(message: AssignTournamentRequest): unknown {
    const obj: any = {};
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.tournamentId !== 0) {
      obj.tournamentId = Math.round(message.tournamentId);
    }
    return obj;
  },

  create(base?: DeepPartial<AssignTournamentRequest>): AssignTournamentRequest {
    return AssignTournamentRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AssignTournamentRequest>): AssignTournamentRequest {
    const message = createBaseAssignTournamentRequest();
    message.deviceId = object.deviceId ?? 0;
    message.tournamentId = object.tournamentId ?? 0;
    return message;
  },
};

function createBaseSetTournamentIdsRequest(): SetTournamentIdsRequest {
  return { tournamentIds: [] };
}

export const SetTournamentIdsRequest: MessageFns<SetTournamentIdsRequest> = {
  encode(message: SetTournamentIdsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.tournamentIds) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetTournamentIdsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetTournamentIdsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.tournamentIds.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.tournamentIds.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetTournamentIdsRequest {
    return {
      tournamentIds: globalThis.Array.isArray(object?.tournamentIds)
        ? object.tournamentIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.tournament_ids)
        ? object.tournament_ids.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: SetTournamentIdsRequest): unknown {
    const obj: any = {};
    if (message.tournamentIds?.length) {
      obj.tournamentIds = message.tournamentIds.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SetTournamentIdsRequest>): SetTournamentIdsRequest {
    return SetTournamentIdsRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetTournamentIdsRequest>): SetTournamentIdsRequest {
    const message = createBaseSetTournamentIdsRequest();
    message.tournamentIds = object.tournamentIds?.map((e) => e) || [];
    return message;
  },
};

function createBaseDeviceTournament(): DeviceTournament {
  return {
    id: 0,
    createdAt: "",
    updatedAt: "",
    deviceId: 0,
    tournamentId: 0,
    device: undefined,
    tournament: undefined,
  };
}

export const DeviceTournament: MessageFns<DeviceTournament> = {
  encode(message: DeviceTournament, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.deviceId !== 0) {
      writer.uint32(32).uint32(message.deviceId);
    }
    if (message.tournamentId !== 0) {
      writer.uint32(40).uint32(message.tournamentId);
    }
    if (message.device !== undefined) {
      Device.encode(message.device, writer.uint32(50).fork()).join();
    }
    if (message.tournament !== undefined) {
      Tournament.encode(message.tournament, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceTournament {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceTournament();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deviceId = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.tournamentId = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.device = Device.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tournament = Tournament.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceTournament {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
      tournamentId: isSet(object.tournamentId)
        ? globalThis.Number(object.tournamentId)
        : isSet(object.tournament_id)
        ? globalThis.Number(object.tournament_id)
        : 0,
      device: isSet(object.device) ? Device.fromJSON(object.device) : undefined,
      tournament: isSet(object.tournament) ? Tournament.fromJSON(object.tournament) : undefined,
    };
  },

  toJSON(message: DeviceTournament): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.tournamentId !== 0) {
      obj.tournamentId = Math.round(message.tournamentId);
    }
    if (message.device !== undefined) {
      obj.device = Device.toJSON(message.device);
    }
    if (message.tournament !== undefined) {
      obj.tournament = Tournament.toJSON(message.tournament);
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceTournament>): DeviceTournament {
    return DeviceTournament.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceTournament>): DeviceTournament {
    const message = createBaseDeviceTournament();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.deviceId = object.deviceId ?? 0;
    message.tournamentId = object.tournamentId ?? 0;
    message.device = (object.device !== undefined && object.device !== null)
      ? Device.fromPartial(object.device)
      : undefined;
    message.tournament = (object.tournament !== undefined && object.tournament !== null)
      ? Tournament.fromPartial(object.tournament)
      : undefined;
    return message;
  },
};

function createBaseDeviceTournamentList(): DeviceTournamentList {
  return { deviceTournaments: [] };
}

export const DeviceTournamentList: MessageFns<DeviceTournamentList> = {
  encode(message: DeviceTournamentList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.deviceTournaments) {
      DeviceTournament.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceTournamentList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceTournamentList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceTournaments.push(DeviceTournament.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceTournamentList {
    return {
      deviceTournaments: globalThis.Array.isArray(object?.deviceTournaments)
        ? object.deviceTournaments.map((e: any) => DeviceTournament.fromJSON(e))
        : globalThis.Array.isArray(object?.device_tournaments)
        ? object.device_tournaments.map((e: any) => DeviceTournament.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DeviceTournamentList): unknown {
    const obj: any = {};
    if (message.deviceTournaments?.length) {
      obj.deviceTournaments = message.deviceTournaments.map((e) => DeviceTournament.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceTournamentList>): DeviceTournamentList {
    return DeviceTournamentList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceTournamentList>): DeviceTournamentList {
    const message = createBaseDeviceTournamentList();
    message.deviceTournaments = object.deviceTournaments?.map((e) => DeviceTournament.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeviceTournamentPage(): DeviceTournamentPage {
  return { data: [], page: undefined };
}

export const DeviceTournamentPage: MessageFns<DeviceTournamentPage> = {
  encode(message: DeviceTournamentPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      DeviceTournament.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceTournamentPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceTournamentPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(DeviceTournament.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceTournamentPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => DeviceTournament.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: DeviceTournamentPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => DeviceTournament.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<DeviceTournamentPage>): DeviceTournamentPage {
    return DeviceTournamentPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<DeviceTournamentPage>): DeviceTournamentPage {
    const message = createBaseDeviceTournamentPage();
    message.data = object.data?.map((e) => DeviceTournament.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseGlobalTournamentConfig(): GlobalTournamentConfig {
  return { id: 0, createdAt: "", updatedAt: "", tournamentId: 0, tournament: undefined };
}

export const GlobalTournamentConfig: MessageFns<GlobalTournamentConfig> = {
  encode(message: GlobalTournamentConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.tournamentId !== 0) {
      writer.uint32(32).uint32(message.tournamentId);
    }
    if (message.tournament !== undefined) {
      Tournament.encode(message.tournament, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GlobalTournamentConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGlobalTournamentConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.tournamentId = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.tournament = Tournament.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GlobalTournamentConfig {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      tournamentId: isSet(object.tournamentId)
        ? globalThis.Number(object.tournamentId)
        : isSet(object.tournament_id)
        ? globalThis.Number(object.tournament_id)
        : 0,
      tournament: isSet(object.tournament) ? Tournament.fromJSON(object.tournament) : undefined,
    };
  },

  toJSON(message: GlobalTournamentConfig): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.tournamentId !== 0) {
      obj.tournamentId = Math.round(message.tournamentId);
    }
    if (message.tournament !== undefined) {
      obj.tournament = Tournament.toJSON(message.tournament);
    }
    return obj;
  },

  create(base?: DeepPartial<GlobalTournamentConfig>): GlobalTournamentConfig {
    return GlobalTournamentConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GlobalTournamentConfig>): GlobalTournamentConfig {
    const message = createBaseGlobalTournamentConfig();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.tournamentId = object.tournamentId ?? 0;
    message.tournament = (object.tournament !== undefined && object.tournament !== null)
      ? Tournament.fromPartial(object.tournament)
      : undefined;
    return message;
  },
};

function createBaseGlobalTournamentConfigList(): GlobalTournamentConfigList {
  return { configs: [] };
}

export const GlobalTournamentConfigList: MessageFns<GlobalTournamentConfigList> = {
  encode(message: GlobalTournamentConfigList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.configs) {
      GlobalTournamentConfig.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GlobalTournamentConfigList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGlobalTournamentConfigList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.configs.push(GlobalTournamentConfig.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GlobalTournamentConfigList {
    return {
      configs: globalThis.Array.isArray(object?.configs)
        ? object.configs.map((e: any) => GlobalTournamentConfig.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GlobalTournamentConfigList): unknown {
    const obj: any = {};
    if (message.configs?.length) {
      obj.configs = message.configs.map((e) => GlobalTournamentConfig.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<GlobalTournamentConfigList>): GlobalTournamentConfigList {
    return GlobalTournamentConfigList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GlobalTournamentConfigList>): GlobalTournamentConfigList {
    const message = createBaseGlobalTournamentConfigList();
    message.configs = object.configs?.map((e) => GlobalTournamentConfig.fromPartial(e)) || [];
    return message;
  },
};

function createBaseTeam(): Team {
  return { id: 0, teamId: 0, logoUrl: "", primaryColor: "", secondaryColor: "", textColor: "", name: "" };
}

export const Team: MessageFns<Team> = {
  encode(message: Team, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.teamId !== 0) {
      writer.uint32(16).int64(message.teamId);
    }
    if (message.logoUrl !== "") {
      writer.uint32(26).string(message.logoUrl);
    }
    if (message.primaryColor !== "") {
      writer.uint32(34).string(message.primaryColor);
    }
    if (message.secondaryColor !== "") {
      writer.uint32(42).string(message.secondaryColor);
    }
    if (message.textColor !== "") {
      writer.uint32(50).string(message.textColor);
    }
    if (message.name !== "") {
      writer.uint32(58).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Team {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTeam();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.teamId = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.logoUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.primaryColor = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.secondaryColor = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.textColor = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Team {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      teamId: isSet(object.teamId)
        ? globalThis.Number(object.teamId)
        : isSet(object.team_id)
        ? globalThis.Number(object.team_id)
        : 0,
      logoUrl: isSet(object.logoUrl)
        ? globalThis.String(object.logoUrl)
        : isSet(object.logo_url)
        ? globalThis.String(object.logo_url)
        : "",
      primaryColor: isSet(object.primaryColor)
        ? globalThis.String(object.primaryColor)
        : isSet(object.primary_color)
        ? globalThis.String(object.primary_color)
        : "",
      secondaryColor: isSet(object.secondaryColor)
        ? globalThis.String(object.secondaryColor)
        : isSet(object.secondary_color)
        ? globalThis.String(object.secondary_color)
        : "",
      textColor: isSet(object.textColor)
        ? globalThis.String(object.textColor)
        : isSet(object.text_color)
        ? globalThis.String(object.text_color)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: Team): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.teamId !== 0) {
      obj.teamId = Math.round(message.teamId);
    }
    if (message.logoUrl !== "") {
      obj.logoUrl = message.logoUrl;
    }
    if (message.primaryColor !== "") {
      obj.primaryColor = message.primaryColor;
    }
    if (message.secondaryColor !== "") {
      obj.secondaryColor = message.secondaryColor;
    }
    if (message.textColor !== "") {
      obj.textColor = message.textColor;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<Team>): Team {
    return Team.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Team>): Team {
    const message = createBaseTeam();
    message.id = object.id ?? 0;
    message.teamId = object.teamId ?? 0;
    message.logoUrl = object.logoUrl ?? "";
    message.primaryColor = object.primaryColor ?? "";
    message.secondaryColor = object.secondaryColor ?? "";
    message.textColor = object.textColor ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseSofaScoreEvent(): SofaScoreEvent {
  return {
    id: 0,
    createdAt: "",
    updatedAt: "",
    sofaScoreEventId: 0,
    sport: "",
    homeScore: 0,
    homeTeamId: 0,
    awayScore: 0,
    awayTeamId: 0,
    scrapedAt: 0,
    category: "",
    startTimestamp: 0,
    currentPeriodStartTimestamp: 0,
    slug: "",
    teamHome: undefined,
    teamAway: undefined,
    league: undefined,
    statusType: "",
  };
}

export const SofaScoreEvent: MessageFns<SofaScoreEvent> = {
  encode(message: SofaScoreEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.sofaScoreEventId !== 0) {
      writer.uint32(32).int64(message.sofaScoreEventId);
    }
    if (message.sport !== "") {
      writer.uint32(42).string(message.sport);
    }
    if (message.homeScore !== 0) {
      writer.uint32(48).int32(message.homeScore);
    }
    if (message.homeTeamId !== 0) {
      writer.uint32(56).int64(message.homeTeamId);
    }
    if (message.awayScore !== 0) {
      writer.uint32(64).int32(message.awayScore);
    }
    if (message.awayTeamId !== 0) {
      writer.uint32(72).int64(message.awayTeamId);
    }
    if (message.scrapedAt !== 0) {
      writer.uint32(80).int64(message.scrapedAt);
    }
    if (message.category !== "") {
      writer.uint32(90).string(message.category);
    }
    if (message.startTimestamp !== 0) {
      writer.uint32(96).int64(message.startTimestamp);
    }
    if (message.currentPeriodStartTimestamp !== 0) {
      writer.uint32(104).int64(message.currentPeriodStartTimestamp);
    }
    if (message.slug !== "") {
      writer.uint32(114).string(message.slug);
    }
    if (message.teamHome !== undefined) {
      Team.encode(message.teamHome, writer.uint32(122).fork()).join();
    }
    if (message.teamAway !== undefined) {
      Team.encode(message.teamAway, writer.uint32(130).fork()).join();
    }
    if (message.league !== undefined) {
      Tournament.encode(message.league, writer.uint32(138).fork()).join();
    }
    if (message.statusType !== "") {
      writer.uint32(146).string(message.statusType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SofaScoreEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSofaScoreEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.sofaScoreEventId = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sport = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.homeScore = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.homeTeamId = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.awayScore = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.awayTeamId = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.scrapedAt = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.category = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.startTimestamp = longToNumber(reader.int64());
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.currentPeriodStartTimestamp = longToNumber(reader.int64());
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.teamHome = Team.decode(reader, reader.uint32());
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.teamAway = Team.decode(reader, reader.uint32());
          continue;
        }
        case 17: {
          if (tag !== 138) {
            break;
          }

          message.league = Tournament.decode(reader, reader.uint32());
          continue;
        }
        case 18: {
          if (tag !== 146) {
            break;
          }

          message.statusType = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SofaScoreEvent {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      sofaScoreEventId: isSet(object.sofaScoreEventId)
        ? globalThis.Number(object.sofaScoreEventId)
        : isSet(object.sofa_score_event_id)
        ? globalThis.Number(object.sofa_score_event_id)
        : 0,
      sport: isSet(object.sport) ? globalThis.String(object.sport) : "",
      homeScore: isSet(object.homeScore)
        ? globalThis.Number(object.homeScore)
        : isSet(object.home_score)
        ? globalThis.Number(object.home_score)
        : 0,
      homeTeamId: isSet(object.homeTeamId)
        ? globalThis.Number(object.homeTeamId)
        : isSet(object.home_team_id)
        ? globalThis.Number(object.home_team_id)
        : 0,
      awayScore: isSet(object.awayScore)
        ? globalThis.Number(object.awayScore)
        : isSet(object.away_score)
        ? globalThis.Number(object.away_score)
        : 0,
      awayTeamId: isSet(object.awayTeamId)
        ? globalThis.Number(object.awayTeamId)
        : isSet(object.away_team_id)
        ? globalThis.Number(object.away_team_id)
        : 0,
      scrapedAt: isSet(object.scrapedAt)
        ? globalThis.Number(object.scrapedAt)
        : isSet(object.scraped_at)
        ? globalThis.Number(object.scraped_at)
        : 0,
      category: isSet(object.category) ? globalThis.String(object.category) : "",
      startTimestamp: isSet(object.startTimestamp)
        ? globalThis.Number(object.startTimestamp)
        : isSet(object.start_timestamp)
        ? globalThis.Number(object.start_timestamp)
        : 0,
      currentPeriodStartTimestamp: isSet(object.currentPeriodStartTimestamp)
        ? globalThis.Number(object.currentPeriodStartTimestamp)
        : isSet(object.current_period_start_timestamp)
        ? globalThis.Number(object.current_period_start_timestamp)
        : 0,
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      teamHome: isSet(object.teamHome)
        ? Team.fromJSON(object.teamHome)
        : isSet(object.team_home)
        ? Team.fromJSON(object.team_home)
        : undefined,
      teamAway: isSet(object.teamAway)
        ? Team.fromJSON(object.teamAway)
        : isSet(object.team_away)
        ? Team.fromJSON(object.team_away)
        : undefined,
      league: isSet(object.league) ? Tournament.fromJSON(object.league) : undefined,
      statusType: isSet(object.statusType)
        ? globalThis.String(object.statusType)
        : isSet(object.status_type)
        ? globalThis.String(object.status_type)
        : "",
    };
  },

  toJSON(message: SofaScoreEvent): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.sofaScoreEventId !== 0) {
      obj.sofaScoreEventId = Math.round(message.sofaScoreEventId);
    }
    if (message.sport !== "") {
      obj.sport = message.sport;
    }
    if (message.homeScore !== 0) {
      obj.homeScore = Math.round(message.homeScore);
    }
    if (message.homeTeamId !== 0) {
      obj.homeTeamId = Math.round(message.homeTeamId);
    }
    if (message.awayScore !== 0) {
      obj.awayScore = Math.round(message.awayScore);
    }
    if (message.awayTeamId !== 0) {
      obj.awayTeamId = Math.round(message.awayTeamId);
    }
    if (message.scrapedAt !== 0) {
      obj.scrapedAt = Math.round(message.scrapedAt);
    }
    if (message.category !== "") {
      obj.category = message.category;
    }
    if (message.startTimestamp !== 0) {
      obj.startTimestamp = Math.round(message.startTimestamp);
    }
    if (message.currentPeriodStartTimestamp !== 0) {
      obj.currentPeriodStartTimestamp = Math.round(message.currentPeriodStartTimestamp);
    }
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.teamHome !== undefined) {
      obj.teamHome = Team.toJSON(message.teamHome);
    }
    if (message.teamAway !== undefined) {
      obj.teamAway = Team.toJSON(message.teamAway);
    }
    if (message.league !== undefined) {
      obj.league = Tournament.toJSON(message.league);
    }
    if (message.statusType !== "") {
      obj.statusType = message.statusType;
    }
    return obj;
  },

  create(base?: DeepPartial<SofaScoreEvent>): SofaScoreEvent {
    return SofaScoreEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SofaScoreEvent>): SofaScoreEvent {
    const message = createBaseSofaScoreEvent();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.sofaScoreEventId = object.sofaScoreEventId ?? 0;
    message.sport = object.sport ?? "";
    message.homeScore = object.homeScore ?? 0;
    message.homeTeamId = object.homeTeamId ?? 0;
    message.awayScore = object.awayScore ?? 0;
    message.awayTeamId = object.awayTeamId ?? 0;
    message.scrapedAt = object.scrapedAt ?? 0;
    message.category = object.category ?? "";
    message.startTimestamp = object.startTimestamp ?? 0;
    message.currentPeriodStartTimestamp = object.currentPeriodStartTimestamp ?? 0;
    message.slug = object.slug ?? "";
    message.teamHome = (object.teamHome !== undefined && object.teamHome !== null)
      ? Team.fromPartial(object.teamHome)
      : undefined;
    message.teamAway = (object.teamAway !== undefined && object.teamAway !== null)
      ? Team.fromPartial(object.teamAway)
      : undefined;
    message.league = (object.league !== undefined && object.league !== null)
      ? Tournament.fromPartial(object.league)
      : undefined;
    message.statusType = object.statusType ?? "";
    return message;
  },
};

function createBaseEventsList(): EventsList {
  return { data: [], page: 0, limit: 0, total: 0, totalPages: 0 };
}

export const EventsList: MessageFns<EventsList> = {
  encode(message: EventsList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      SofaScoreEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== 0) {
      writer.uint32(16).int32(message.page);
    }
    if (message.limit !== 0) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.total !== 0) {
      writer.uint32(32).int64(message.total);
    }
    if (message.totalPages !== 0) {
      writer.uint32(40).int32(message.totalPages);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventsList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventsList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(SofaScoreEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.page = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.total = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalPages = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventsList {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => SofaScoreEvent.fromJSON(e)) : [],
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
      totalPages: isSet(object.totalPages)
        ? globalThis.Number(object.totalPages)
        : isSet(object.total_pages)
        ? globalThis.Number(object.total_pages)
        : 0,
    };
  },

  toJSON(message: EventsList): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => SofaScoreEvent.toJSON(e));
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    if (message.totalPages !== 0) {
      obj.totalPages = Math.round(message.totalPages);
    }
    return obj;
  },

  create(base?: DeepPartial<EventsList>): EventsList {
    return EventsList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventsList>): EventsList {
    const message = createBaseEventsList();
    message.data = object.data?.map((e) => SofaScoreEvent.fromPartial(e)) || [];
    message.page = object.page ?? 0;
    message.limit = object.limit ?? 0;
    message.total = object.total ?? 0;
    message.totalPages = object.totalPages ?? 0;
    return message;
  },
};

function createBaseEventPage(): EventPage {
  return { data: [], page: undefined };
}

export const EventPage: MessageFns<EventPage> = {
  encode(message: EventPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      SofaScoreEvent.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(SofaScoreEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => SofaScoreEvent.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: EventPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => SofaScoreEvent.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<EventPage>): EventPage {
    return EventPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventPage>): EventPage {
    const message = createBaseEventPage();
    message.data = object.data?.map((e) => SofaScoreEvent.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseLogPlaybackRequest(): LogPlaybackRequest {
  return { deviceToken: "", content: "", startedAt: 0 };
}

export const LogPlaybackRequest: MessageFns<LogPlaybackRequest> = {
  encode(message: LogPlaybackRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceToken !== "") {
      writer.uint32(10).string(message.deviceToken);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    if (message.startedAt !== 0) {
      writer.uint32(24).int64(message.startedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LogPlaybackRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLogPlaybackRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.startedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LogPlaybackRequest {
    return {
      deviceToken: isSet(object.deviceToken)
        ? globalThis.String(object.deviceToken)
        : isSet(object.device_token)
        ? globalThis.String(object.device_token)
        : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      startedAt: isSet(object.startedAt)
        ? globalThis.Number(object.startedAt)
        : isSet(object.started_at)
        ? globalThis.Number(object.started_at)
        : 0,
    };
  },

  toJSON(message: LogPlaybackRequest): unknown {
    const obj: any = {};
    if (message.deviceToken !== "") {
      obj.deviceToken = message.deviceToken;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.startedAt !== 0) {
      obj.startedAt = Math.round(message.startedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<LogPlaybackRequest>): LogPlaybackRequest {
    return LogPlaybackRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LogPlaybackRequest>): LogPlaybackRequest {
    const message = createBaseLogPlaybackRequest();
    message.deviceToken = object.deviceToken ?? "";
    message.content = object.content ?? "";
    message.startedAt = object.startedAt ?? 0;
    return message;
  },
};

function createBaseUpdatePlaybackRequest(): UpdatePlaybackRequest {
  return { endedAt: 0 };
}

export const UpdatePlaybackRequest: MessageFns<UpdatePlaybackRequest> = {
  encode(message: UpdatePlaybackRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.endedAt !== 0) {
      writer.uint32(8).int64(message.endedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdatePlaybackRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdatePlaybackRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.endedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdatePlaybackRequest {
    return {
      endedAt: isSet(object.endedAt)
        ? globalThis.Number(object.endedAt)
        : isSet(object.ended_at)
        ? globalThis.Number(object.ended_at)
        : 0,
    };
  },

  toJSON(message: UpdatePlaybackRequest): unknown {
    const obj: any = {};
    if (message.endedAt !== 0) {
      obj.endedAt = Math.round(message.endedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdatePlaybackRequest>): UpdatePlaybackRequest {
    return UpdatePlaybackRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdatePlaybackRequest>): UpdatePlaybackRequest {
    const message = createBaseUpdatePlaybackRequest();
    message.endedAt = object.endedAt ?? 0;
    return message;
  },
};

function createBasePlaybackLog(): PlaybackLog {
  return { id: 0, createdAt: "", updatedAt: "", deviceId: 0, content: "", startedAt: 0, endedAt: 0 };
}

export const PlaybackLog: MessageFns<PlaybackLog> = {
  encode(message: PlaybackLog, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.deviceId !== 0) {
      writer.uint32(32).uint32(message.deviceId);
    }
    if (message.content !== "") {
      writer.uint32(42).string(message.content);
    }
    if (message.startedAt !== 0) {
      writer.uint32(48).int64(message.startedAt);
    }
    if (message.endedAt !== 0) {
      writer.uint32(56).int64(message.endedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaybackLog {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaybackLog();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deviceId = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.startedAt = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.endedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaybackLog {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      startedAt: isSet(object.startedAt)
        ? globalThis.Number(object.startedAt)
        : isSet(object.started_at)
        ? globalThis.Number(object.started_at)
        : 0,
      endedAt: isSet(object.endedAt)
        ? globalThis.Number(object.endedAt)
        : isSet(object.ended_at)
        ? globalThis.Number(object.ended_at)
        : 0,
    };
  },

  toJSON(message: PlaybackLog): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.startedAt !== 0) {
      obj.startedAt = Math.round(message.startedAt);
    }
    if (message.endedAt !== 0) {
      obj.endedAt = Math.round(message.endedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaybackLog>): PlaybackLog {
    return PlaybackLog.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaybackLog>): PlaybackLog {
    const message = createBasePlaybackLog();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.deviceId = object.deviceId ?? 0;
    message.content = object.content ?? "";
    message.startedAt = object.startedAt ?? 0;
    message.endedAt = object.endedAt ?? 0;
    return message;
  },
};

function createBasePlaybackLogList(): PlaybackLogList {
  return { list: [], total: 0 };
}

export const PlaybackLogList: MessageFns<PlaybackLogList> = {
  encode(message: PlaybackLogList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.list) {
      PlaybackLog.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== 0) {
      writer.uint32(16).uint32(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaybackLogList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaybackLogList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.list.push(PlaybackLog.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaybackLogList {
    return {
      list: globalThis.Array.isArray(object?.list) ? object.list.map((e: any) => PlaybackLog.fromJSON(e)) : [],
      total: isSet(object.total) ? globalThis.Number(object.total) : 0,
    };
  },

  toJSON(message: PlaybackLogList): unknown {
    const obj: any = {};
    if (message.list?.length) {
      obj.list = message.list.map((e) => PlaybackLog.toJSON(e));
    }
    if (message.total !== 0) {
      obj.total = Math.round(message.total);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaybackLogList>): PlaybackLogList {
    return PlaybackLogList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaybackLogList>): PlaybackLogList {
    const message = createBasePlaybackLogList();
    message.list = object.list?.map((e) => PlaybackLog.fromPartial(e)) || [];
    message.total = object.total ?? 0;
    return message;
  },
};

function createBasePlaybackPage(): PlaybackPage {
  return { data: [], page: undefined };
}

export const PlaybackPage: MessageFns<PlaybackPage> = {
  encode(message: PlaybackPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      PlaybackLog.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlaybackPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlaybackPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(PlaybackLog.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlaybackPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => PlaybackLog.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: PlaybackPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => PlaybackLog.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<PlaybackPage>): PlaybackPage {
    return PlaybackPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlaybackPage>): PlaybackPage {
    const message = createBasePlaybackPage();
    message.data = object.data?.map((e) => PlaybackLog.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseEventStats(): EventStats {
  return { sofaScoreEventId: 0, viewCount: 0 };
}

export const EventStats: MessageFns<EventStats> = {
  encode(message: EventStats, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sofaScoreEventId !== 0) {
      writer.uint32(8).int64(message.sofaScoreEventId);
    }
    if (message.viewCount !== 0) {
      writer.uint32(16).int64(message.viewCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventStats {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEventStats();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sofaScoreEventId = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.viewCount = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EventStats {
    return {
      sofaScoreEventId: isSet(object.sofaScoreEventId)
        ? globalThis.Number(object.sofaScoreEventId)
        : isSet(object.sofa_score_event_id)
        ? globalThis.Number(object.sofa_score_event_id)
        : 0,
      viewCount: isSet(object.viewCount)
        ? globalThis.Number(object.viewCount)
        : isSet(object.view_count)
        ? globalThis.Number(object.view_count)
        : 0,
    };
  },

  toJSON(message: EventStats): unknown {
    const obj: any = {};
    if (message.sofaScoreEventId !== 0) {
      obj.sofaScoreEventId = Math.round(message.sofaScoreEventId);
    }
    if (message.viewCount !== 0) {
      obj.viewCount = Math.round(message.viewCount);
    }
    return obj;
  },

  create(base?: DeepPartial<EventStats>): EventStats {
    return EventStats.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EventStats>): EventStats {
    const message = createBaseEventStats();
    message.sofaScoreEventId = object.sofaScoreEventId ?? 0;
    message.viewCount = object.viewCount ?? 0;
    return message;
  },
};

function createBaseTopEventsResponse(): TopEventsResponse {
  return { stats: [] };
}

export const TopEventsResponse: MessageFns<TopEventsResponse> = {
  encode(message: TopEventsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.stats) {
      EventStats.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TopEventsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTopEventsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stats.push(EventStats.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TopEventsResponse {
    return {
      stats: globalThis.Array.isArray(object?.stats) ? object.stats.map((e: any) => EventStats.fromJSON(e)) : [],
    };
  },

  toJSON(message: TopEventsResponse): unknown {
    const obj: any = {};
    if (message.stats?.length) {
      obj.stats = message.stats.map((e) => EventStats.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TopEventsResponse>): TopEventsResponse {
    return TopEventsResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TopEventsResponse>): TopEventsResponse {
    const message = createBaseTopEventsResponse();
    message.stats = object.stats?.map((e) => EventStats.fromPartial(e)) || [];
    return message;
  },
};

function createBaseApkInfo(): ApkInfo {
  return {
    id: 0,
    version: "",
    fileName: "",
    fileSize: 0,
    description: "",
    packageName: "",
    versionCode: 0,
    minSdkVersion: 0,
    targetSdkVersion: 0,
    downloadToken: "",
    downloadUrl: "",
    createdAt: "",
    isActive: false,
    downloads: 0,
    panelUrl: "",
  };
}

export const ApkInfo: MessageFns<ApkInfo> = {
  encode(message: ApkInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.fileName !== "") {
      writer.uint32(26).string(message.fileName);
    }
    if (message.fileSize !== 0) {
      writer.uint32(32).int64(message.fileSize);
    }
    if (message.description !== "") {
      writer.uint32(42).string(message.description);
    }
    if (message.packageName !== "") {
      writer.uint32(50).string(message.packageName);
    }
    if (message.versionCode !== 0) {
      writer.uint32(56).int32(message.versionCode);
    }
    if (message.minSdkVersion !== 0) {
      writer.uint32(64).int32(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      writer.uint32(72).int32(message.targetSdkVersion);
    }
    if (message.downloadToken !== "") {
      writer.uint32(82).string(message.downloadToken);
    }
    if (message.downloadUrl !== "") {
      writer.uint32(90).string(message.downloadUrl);
    }
    if (message.createdAt !== "") {
      writer.uint32(98).string(message.createdAt);
    }
    if (message.isActive !== false) {
      writer.uint32(104).bool(message.isActive);
    }
    if (message.downloads !== 0) {
      writer.uint32(112).int32(message.downloads);
    }
    if (message.panelUrl !== "") {
      writer.uint32(122).string(message.panelUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApkInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApkInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fileSize = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.packageName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.versionCode = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minSdkVersion = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.targetSdkVersion = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.downloadToken = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.downloadUrl = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 14: {
          if (tag !== 112) {
            break;
          }

          message.downloads = reader.int32();
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.panelUrl = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApkInfo {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      fileSize: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      packageName: isSet(object.packageName)
        ? globalThis.String(object.packageName)
        : isSet(object.package_name)
        ? globalThis.String(object.package_name)
        : "",
      versionCode: isSet(object.versionCode)
        ? globalThis.Number(object.versionCode)
        : isSet(object.version_code)
        ? globalThis.Number(object.version_code)
        : 0,
      minSdkVersion: isSet(object.minSdkVersion)
        ? globalThis.Number(object.minSdkVersion)
        : isSet(object.min_sdk_version)
        ? globalThis.Number(object.min_sdk_version)
        : 0,
      targetSdkVersion: isSet(object.targetSdkVersion)
        ? globalThis.Number(object.targetSdkVersion)
        : isSet(object.target_sdk_version)
        ? globalThis.Number(object.target_sdk_version)
        : 0,
      downloadToken: isSet(object.downloadToken)
        ? globalThis.String(object.downloadToken)
        : isSet(object.download_token)
        ? globalThis.String(object.download_token)
        : "",
      downloadUrl: isSet(object.downloadUrl)
        ? globalThis.String(object.downloadUrl)
        : isSet(object.download_url)
        ? globalThis.String(object.download_url)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      isActive: isSet(object.isActive)
        ? globalThis.Boolean(object.isActive)
        : isSet(object.is_active)
        ? globalThis.Boolean(object.is_active)
        : false,
      downloads: isSet(object.downloads) ? globalThis.Number(object.downloads) : 0,
      panelUrl: isSet(object.panelUrl)
        ? globalThis.String(object.panelUrl)
        : isSet(object.panel_url)
        ? globalThis.String(object.panel_url)
        : "",
    };
  },

  toJSON(message: ApkInfo): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.packageName !== "") {
      obj.packageName = message.packageName;
    }
    if (message.versionCode !== 0) {
      obj.versionCode = Math.round(message.versionCode);
    }
    if (message.minSdkVersion !== 0) {
      obj.minSdkVersion = Math.round(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      obj.targetSdkVersion = Math.round(message.targetSdkVersion);
    }
    if (message.downloadToken !== "") {
      obj.downloadToken = message.downloadToken;
    }
    if (message.downloadUrl !== "") {
      obj.downloadUrl = message.downloadUrl;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.downloads !== 0) {
      obj.downloads = Math.round(message.downloads);
    }
    if (message.panelUrl !== "") {
      obj.panelUrl = message.panelUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<ApkInfo>): ApkInfo {
    return ApkInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApkInfo>): ApkInfo {
    const message = createBaseApkInfo();
    message.id = object.id ?? 0;
    message.version = object.version ?? "";
    message.fileName = object.fileName ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.description = object.description ?? "";
    message.packageName = object.packageName ?? "";
    message.versionCode = object.versionCode ?? 0;
    message.minSdkVersion = object.minSdkVersion ?? 0;
    message.targetSdkVersion = object.targetSdkVersion ?? 0;
    message.downloadToken = object.downloadToken ?? "";
    message.downloadUrl = object.downloadUrl ?? "";
    message.createdAt = object.createdAt ?? "";
    message.isActive = object.isActive ?? false;
    message.downloads = object.downloads ?? 0;
    message.panelUrl = object.panelUrl ?? "";
    return message;
  },
};

function createBaseApkList(): ApkList {
  return { versions: [] };
}

export const ApkList: MessageFns<ApkList> = {
  encode(message: ApkList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.versions) {
      ApkInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApkList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApkList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.versions.push(ApkInfo.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApkList {
    return {
      versions: globalThis.Array.isArray(object?.versions) ? object.versions.map((e: any) => ApkInfo.fromJSON(e)) : [],
    };
  },

  toJSON(message: ApkList): unknown {
    const obj: any = {};
    if (message.versions?.length) {
      obj.versions = message.versions.map((e) => ApkInfo.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ApkList>): ApkList {
    return ApkList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApkList>): ApkList {
    const message = createBaseApkList();
    message.versions = object.versions?.map((e) => ApkInfo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseApkPage(): ApkPage {
  return { data: [], page: undefined };
}

export const ApkPage: MessageFns<ApkPage> = {
  encode(message: ApkPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      ApkInfo.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApkPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApkPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(ApkInfo.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApkPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => ApkInfo.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ApkPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => ApkInfo.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ApkPage>): ApkPage {
    return ApkPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApkPage>): ApkPage {
    const message = createBaseApkPage();
    message.data = object.data?.map((e) => ApkInfo.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseApkUploadResponse(): ApkUploadResponse {
  return {
    id: 0,
    version: "",
    fileName: "",
    fileSize: 0,
    description: "",
    packageName: "",
    versionCode: 0,
    minSdkVersion: 0,
    targetSdkVersion: 0,
    downloadToken: "",
    downloadUrl: "",
    createdAt: "",
  };
}

export const ApkUploadResponse: MessageFns<ApkUploadResponse> = {
  encode(message: ApkUploadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.fileName !== "") {
      writer.uint32(26).string(message.fileName);
    }
    if (message.fileSize !== 0) {
      writer.uint32(32).int64(message.fileSize);
    }
    if (message.description !== "") {
      writer.uint32(42).string(message.description);
    }
    if (message.packageName !== "") {
      writer.uint32(50).string(message.packageName);
    }
    if (message.versionCode !== 0) {
      writer.uint32(56).int32(message.versionCode);
    }
    if (message.minSdkVersion !== 0) {
      writer.uint32(64).int32(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      writer.uint32(72).int32(message.targetSdkVersion);
    }
    if (message.downloadToken !== "") {
      writer.uint32(82).string(message.downloadToken);
    }
    if (message.downloadUrl !== "") {
      writer.uint32(90).string(message.downloadUrl);
    }
    if (message.createdAt !== "") {
      writer.uint32(98).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApkUploadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApkUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fileSize = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.packageName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.versionCode = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minSdkVersion = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.targetSdkVersion = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.downloadToken = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.downloadUrl = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApkUploadResponse {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      fileSize: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      packageName: isSet(object.packageName)
        ? globalThis.String(object.packageName)
        : isSet(object.package_name)
        ? globalThis.String(object.package_name)
        : "",
      versionCode: isSet(object.versionCode)
        ? globalThis.Number(object.versionCode)
        : isSet(object.version_code)
        ? globalThis.Number(object.version_code)
        : 0,
      minSdkVersion: isSet(object.minSdkVersion)
        ? globalThis.Number(object.minSdkVersion)
        : isSet(object.min_sdk_version)
        ? globalThis.Number(object.min_sdk_version)
        : 0,
      targetSdkVersion: isSet(object.targetSdkVersion)
        ? globalThis.Number(object.targetSdkVersion)
        : isSet(object.target_sdk_version)
        ? globalThis.Number(object.target_sdk_version)
        : 0,
      downloadToken: isSet(object.downloadToken)
        ? globalThis.String(object.downloadToken)
        : isSet(object.download_token)
        ? globalThis.String(object.download_token)
        : "",
      downloadUrl: isSet(object.downloadUrl)
        ? globalThis.String(object.downloadUrl)
        : isSet(object.download_url)
        ? globalThis.String(object.download_url)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
    };
  },

  toJSON(message: ApkUploadResponse): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.packageName !== "") {
      obj.packageName = message.packageName;
    }
    if (message.versionCode !== 0) {
      obj.versionCode = Math.round(message.versionCode);
    }
    if (message.minSdkVersion !== 0) {
      obj.minSdkVersion = Math.round(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      obj.targetSdkVersion = Math.round(message.targetSdkVersion);
    }
    if (message.downloadToken !== "") {
      obj.downloadToken = message.downloadToken;
    }
    if (message.downloadUrl !== "") {
      obj.downloadUrl = message.downloadUrl;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create(base?: DeepPartial<ApkUploadResponse>): ApkUploadResponse {
    return ApkUploadResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApkUploadResponse>): ApkUploadResponse {
    const message = createBaseApkUploadResponse();
    message.id = object.id ?? 0;
    message.version = object.version ?? "";
    message.fileName = object.fileName ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.description = object.description ?? "";
    message.packageName = object.packageName ?? "";
    message.versionCode = object.versionCode ?? 0;
    message.minSdkVersion = object.minSdkVersion ?? 0;
    message.targetSdkVersion = object.targetSdkVersion ?? 0;
    message.downloadToken = object.downloadToken ?? "";
    message.downloadUrl = object.downloadUrl ?? "";
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

function createBaseApkUpdateCheckResponse(): ApkUpdateCheckResponse {
  return {
    updateAvailable: false,
    latestVersion: "",
    packageName: "",
    versionCode: 0,
    downloadUrl: "",
    description: "",
    fileSize: 0,
    minSdkVersion: 0,
    targetSdkVersion: 0,
  };
}

export const ApkUpdateCheckResponse: MessageFns<ApkUpdateCheckResponse> = {
  encode(message: ApkUpdateCheckResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.updateAvailable !== false) {
      writer.uint32(8).bool(message.updateAvailable);
    }
    if (message.latestVersion !== "") {
      writer.uint32(18).string(message.latestVersion);
    }
    if (message.packageName !== "") {
      writer.uint32(26).string(message.packageName);
    }
    if (message.versionCode !== 0) {
      writer.uint32(32).int32(message.versionCode);
    }
    if (message.downloadUrl !== "") {
      writer.uint32(42).string(message.downloadUrl);
    }
    if (message.description !== "") {
      writer.uint32(50).string(message.description);
    }
    if (message.fileSize !== 0) {
      writer.uint32(56).int64(message.fileSize);
    }
    if (message.minSdkVersion !== 0) {
      writer.uint32(64).int32(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      writer.uint32(72).int32(message.targetSdkVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApkUpdateCheckResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApkUpdateCheckResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.updateAvailable = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.latestVersion = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.packageName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.versionCode = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.downloadUrl = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.fileSize = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minSdkVersion = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.targetSdkVersion = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApkUpdateCheckResponse {
    return {
      updateAvailable: isSet(object.updateAvailable)
        ? globalThis.Boolean(object.updateAvailable)
        : isSet(object.update_available)
        ? globalThis.Boolean(object.update_available)
        : false,
      latestVersion: isSet(object.latestVersion)
        ? globalThis.String(object.latestVersion)
        : isSet(object.latest_version)
        ? globalThis.String(object.latest_version)
        : "",
      packageName: isSet(object.packageName)
        ? globalThis.String(object.packageName)
        : isSet(object.package_name)
        ? globalThis.String(object.package_name)
        : "",
      versionCode: isSet(object.versionCode)
        ? globalThis.Number(object.versionCode)
        : isSet(object.version_code)
        ? globalThis.Number(object.version_code)
        : 0,
      downloadUrl: isSet(object.downloadUrl)
        ? globalThis.String(object.downloadUrl)
        : isSet(object.download_url)
        ? globalThis.String(object.download_url)
        : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      fileSize: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
      minSdkVersion: isSet(object.minSdkVersion)
        ? globalThis.Number(object.minSdkVersion)
        : isSet(object.min_sdk_version)
        ? globalThis.Number(object.min_sdk_version)
        : 0,
      targetSdkVersion: isSet(object.targetSdkVersion)
        ? globalThis.Number(object.targetSdkVersion)
        : isSet(object.target_sdk_version)
        ? globalThis.Number(object.target_sdk_version)
        : 0,
    };
  },

  toJSON(message: ApkUpdateCheckResponse): unknown {
    const obj: any = {};
    if (message.updateAvailable !== false) {
      obj.updateAvailable = message.updateAvailable;
    }
    if (message.latestVersion !== "") {
      obj.latestVersion = message.latestVersion;
    }
    if (message.packageName !== "") {
      obj.packageName = message.packageName;
    }
    if (message.versionCode !== 0) {
      obj.versionCode = Math.round(message.versionCode);
    }
    if (message.downloadUrl !== "") {
      obj.downloadUrl = message.downloadUrl;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.minSdkVersion !== 0) {
      obj.minSdkVersion = Math.round(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      obj.targetSdkVersion = Math.round(message.targetSdkVersion);
    }
    return obj;
  },

  create(base?: DeepPartial<ApkUpdateCheckResponse>): ApkUpdateCheckResponse {
    return ApkUpdateCheckResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApkUpdateCheckResponse>): ApkUpdateCheckResponse {
    const message = createBaseApkUpdateCheckResponse();
    message.updateAvailable = object.updateAvailable ?? false;
    message.latestVersion = object.latestVersion ?? "";
    message.packageName = object.packageName ?? "";
    message.versionCode = object.versionCode ?? 0;
    message.downloadUrl = object.downloadUrl ?? "";
    message.description = object.description ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.minSdkVersion = object.minSdkVersion ?? 0;
    message.targetSdkVersion = object.targetSdkVersion ?? 0;
    return message;
  },
};

function createBaseApkVersion(): ApkVersion {
  return { id: 0, url: "" };
}

export const ApkVersion: MessageFns<ApkVersion> = {
  encode(message: ApkVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApkVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApkVersion();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApkVersion {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      url: isSet(object.url) ? globalThis.String(object.url) : "",
    };
  },

  toJSON(message: ApkVersion): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    return obj;
  },

  create(base?: DeepPartial<ApkVersion>): ApkVersion {
    return ApkVersion.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApkVersion>): ApkVersion {
    const message = createBaseApkVersion();
    message.id = object.id ?? 0;
    message.url = object.url ?? "";
    return message;
  },
};

function createBaseUploadBeginRequest(): UploadBeginRequest {
  return { fileName: "", fileSize: 0, totalChunks: 0, version: "", description: "" };
}

export const UploadBeginRequest: MessageFns<UploadBeginRequest> = {
  encode(message: UploadBeginRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fileName !== "") {
      writer.uint32(10).string(message.fileName);
    }
    if (message.fileSize !== 0) {
      writer.uint32(16).int64(message.fileSize);
    }
    if (message.totalChunks !== 0) {
      writer.uint32(24).int32(message.totalChunks);
    }
    if (message.version !== "") {
      writer.uint32(34).string(message.version);
    }
    if (message.description !== "") {
      writer.uint32(42).string(message.description);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadBeginRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadBeginRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.fileSize = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalChunks = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadBeginRequest {
    return {
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      fileSize: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
      totalChunks: isSet(object.totalChunks)
        ? globalThis.Number(object.totalChunks)
        : isSet(object.total_chunks)
        ? globalThis.Number(object.total_chunks)
        : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
    };
  },

  toJSON(message: UploadBeginRequest): unknown {
    const obj: any = {};
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.totalChunks !== 0) {
      obj.totalChunks = Math.round(message.totalChunks);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadBeginRequest>): UploadBeginRequest {
    return UploadBeginRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadBeginRequest>): UploadBeginRequest {
    const message = createBaseUploadBeginRequest();
    message.fileName = object.fileName ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.totalChunks = object.totalChunks ?? 0;
    message.version = object.version ?? "";
    message.description = object.description ?? "";
    return message;
  },
};

function createBaseUploadBeginResponse(): UploadBeginResponse {
  return {
    uploadId: "",
    maxChunkSize: 0,
    maxTotalChunks: 0,
    maxAggregateSize: 0,
    expiresAt: 0,
    status: "",
    chunksReceived: 0,
    bytesReceived: 0,
  };
}

export const UploadBeginResponse: MessageFns<UploadBeginResponse> = {
  encode(message: UploadBeginResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uploadId !== "") {
      writer.uint32(10).string(message.uploadId);
    }
    if (message.maxChunkSize !== 0) {
      writer.uint32(16).int32(message.maxChunkSize);
    }
    if (message.maxTotalChunks !== 0) {
      writer.uint32(24).int32(message.maxTotalChunks);
    }
    if (message.maxAggregateSize !== 0) {
      writer.uint32(32).int64(message.maxAggregateSize);
    }
    if (message.expiresAt !== 0) {
      writer.uint32(40).int32(message.expiresAt);
    }
    if (message.status !== "") {
      writer.uint32(50).string(message.status);
    }
    if (message.chunksReceived !== 0) {
      writer.uint32(56).int32(message.chunksReceived);
    }
    if (message.bytesReceived !== 0) {
      writer.uint32(64).int64(message.bytesReceived);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadBeginResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadBeginResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxChunkSize = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxTotalChunks = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxAggregateSize = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.expiresAt = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.chunksReceived = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.bytesReceived = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadBeginResponse {
    return {
      uploadId: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      maxChunkSize: isSet(object.maxChunkSize)
        ? globalThis.Number(object.maxChunkSize)
        : isSet(object.max_chunk_size)
        ? globalThis.Number(object.max_chunk_size)
        : 0,
      maxTotalChunks: isSet(object.maxTotalChunks)
        ? globalThis.Number(object.maxTotalChunks)
        : isSet(object.max_total_chunks)
        ? globalThis.Number(object.max_total_chunks)
        : 0,
      maxAggregateSize: isSet(object.maxAggregateSize)
        ? globalThis.Number(object.maxAggregateSize)
        : isSet(object.max_aggregate_size)
        ? globalThis.Number(object.max_aggregate_size)
        : 0,
      expiresAt: isSet(object.expiresAt)
        ? globalThis.Number(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.Number(object.expires_at)
        : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      chunksReceived: isSet(object.chunksReceived)
        ? globalThis.Number(object.chunksReceived)
        : isSet(object.chunks_received)
        ? globalThis.Number(object.chunks_received)
        : 0,
      bytesReceived: isSet(object.bytesReceived)
        ? globalThis.Number(object.bytesReceived)
        : isSet(object.bytes_received)
        ? globalThis.Number(object.bytes_received)
        : 0,
    };
  },

  toJSON(message: UploadBeginResponse): unknown {
    const obj: any = {};
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    if (message.maxChunkSize !== 0) {
      obj.maxChunkSize = Math.round(message.maxChunkSize);
    }
    if (message.maxTotalChunks !== 0) {
      obj.maxTotalChunks = Math.round(message.maxTotalChunks);
    }
    if (message.maxAggregateSize !== 0) {
      obj.maxAggregateSize = Math.round(message.maxAggregateSize);
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.chunksReceived !== 0) {
      obj.chunksReceived = Math.round(message.chunksReceived);
    }
    if (message.bytesReceived !== 0) {
      obj.bytesReceived = Math.round(message.bytesReceived);
    }
    return obj;
  },

  create(base?: DeepPartial<UploadBeginResponse>): UploadBeginResponse {
    return UploadBeginResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadBeginResponse>): UploadBeginResponse {
    const message = createBaseUploadBeginResponse();
    message.uploadId = object.uploadId ?? "";
    message.maxChunkSize = object.maxChunkSize ?? 0;
    message.maxTotalChunks = object.maxTotalChunks ?? 0;
    message.maxAggregateSize = object.maxAggregateSize ?? 0;
    message.expiresAt = object.expiresAt ?? 0;
    message.status = object.status ?? "";
    message.chunksReceived = object.chunksReceived ?? 0;
    message.bytesReceived = object.bytesReceived ?? 0;
    return message;
  },
};

function createBaseUploadStatusResponse(): UploadStatusResponse {
  return {
    uploadId: "",
    status: "",
    totalChunks: 0,
    chunksReceived: 0,
    bytesReceived: 0,
    expiresAt: 0,
    fileName: "",
    fileSize: 0,
  };
}

export const UploadStatusResponse: MessageFns<UploadStatusResponse> = {
  encode(message: UploadStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uploadId !== "") {
      writer.uint32(10).string(message.uploadId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    if (message.totalChunks !== 0) {
      writer.uint32(24).int32(message.totalChunks);
    }
    if (message.chunksReceived !== 0) {
      writer.uint32(32).int32(message.chunksReceived);
    }
    if (message.bytesReceived !== 0) {
      writer.uint32(40).int64(message.bytesReceived);
    }
    if (message.expiresAt !== 0) {
      writer.uint32(48).int32(message.expiresAt);
    }
    if (message.fileName !== "") {
      writer.uint32(58).string(message.fileName);
    }
    if (message.fileSize !== 0) {
      writer.uint32(64).int64(message.fileSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadStatusResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalChunks = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.chunksReceived = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.bytesReceived = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.expiresAt = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.fileSize = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadStatusResponse {
    return {
      uploadId: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      totalChunks: isSet(object.totalChunks)
        ? globalThis.Number(object.totalChunks)
        : isSet(object.total_chunks)
        ? globalThis.Number(object.total_chunks)
        : 0,
      chunksReceived: isSet(object.chunksReceived)
        ? globalThis.Number(object.chunksReceived)
        : isSet(object.chunks_received)
        ? globalThis.Number(object.chunks_received)
        : 0,
      bytesReceived: isSet(object.bytesReceived)
        ? globalThis.Number(object.bytesReceived)
        : isSet(object.bytes_received)
        ? globalThis.Number(object.bytes_received)
        : 0,
      expiresAt: isSet(object.expiresAt)
        ? globalThis.Number(object.expiresAt)
        : isSet(object.expires_at)
        ? globalThis.Number(object.expires_at)
        : 0,
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      fileSize: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
    };
  },

  toJSON(message: UploadStatusResponse): unknown {
    const obj: any = {};
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.totalChunks !== 0) {
      obj.totalChunks = Math.round(message.totalChunks);
    }
    if (message.chunksReceived !== 0) {
      obj.chunksReceived = Math.round(message.chunksReceived);
    }
    if (message.bytesReceived !== 0) {
      obj.bytesReceived = Math.round(message.bytesReceived);
    }
    if (message.expiresAt !== 0) {
      obj.expiresAt = Math.round(message.expiresAt);
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    return obj;
  },

  create(base?: DeepPartial<UploadStatusResponse>): UploadStatusResponse {
    return UploadStatusResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadStatusResponse>): UploadStatusResponse {
    const message = createBaseUploadStatusResponse();
    message.uploadId = object.uploadId ?? "";
    message.status = object.status ?? "";
    message.totalChunks = object.totalChunks ?? 0;
    message.chunksReceived = object.chunksReceived ?? 0;
    message.bytesReceived = object.bytesReceived ?? 0;
    message.expiresAt = object.expiresAt ?? 0;
    message.fileName = object.fileName ?? "";
    message.fileSize = object.fileSize ?? 0;
    return message;
  },
};

function createBaseUploadChunkResponse(): UploadChunkResponse {
  return { uploadId: "", chunkIndex: 0, ok: false };
}

export const UploadChunkResponse: MessageFns<UploadChunkResponse> = {
  encode(message: UploadChunkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uploadId !== "") {
      writer.uint32(10).string(message.uploadId);
    }
    if (message.chunkIndex !== 0) {
      writer.uint32(16).int32(message.chunkIndex);
    }
    if (message.ok !== false) {
      writer.uint32(24).bool(message.ok);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadChunkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadChunkResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uploadId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.chunkIndex = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ok = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadChunkResponse {
    return {
      uploadId: isSet(object.uploadId)
        ? globalThis.String(object.uploadId)
        : isSet(object.upload_id)
        ? globalThis.String(object.upload_id)
        : "",
      chunkIndex: isSet(object.chunkIndex)
        ? globalThis.Number(object.chunkIndex)
        : isSet(object.chunk_index)
        ? globalThis.Number(object.chunk_index)
        : 0,
      ok: isSet(object.ok) ? globalThis.Boolean(object.ok) : false,
    };
  },

  toJSON(message: UploadChunkResponse): unknown {
    const obj: any = {};
    if (message.uploadId !== "") {
      obj.uploadId = message.uploadId;
    }
    if (message.chunkIndex !== 0) {
      obj.chunkIndex = Math.round(message.chunkIndex);
    }
    if (message.ok !== false) {
      obj.ok = message.ok;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadChunkResponse>): UploadChunkResponse {
    return UploadChunkResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadChunkResponse>): UploadChunkResponse {
    const message = createBaseUploadChunkResponse();
    message.uploadId = object.uploadId ?? "";
    message.chunkIndex = object.chunkIndex ?? 0;
    message.ok = object.ok ?? false;
    return message;
  },
};

function createBaseUploadCompleteResponse(): UploadCompleteResponse {
  return {
    id: 0,
    version: "",
    fileName: "",
    fileSize: 0,
    description: "",
    packageName: "",
    versionCode: 0,
    minSdkVersion: 0,
    targetSdkVersion: 0,
    downloadToken: "",
    downloadUrl: "",
    createdAt: "",
  };
}

export const UploadCompleteResponse: MessageFns<UploadCompleteResponse> = {
  encode(message: UploadCompleteResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.version !== "") {
      writer.uint32(18).string(message.version);
    }
    if (message.fileName !== "") {
      writer.uint32(26).string(message.fileName);
    }
    if (message.fileSize !== 0) {
      writer.uint32(32).int64(message.fileSize);
    }
    if (message.description !== "") {
      writer.uint32(42).string(message.description);
    }
    if (message.packageName !== "") {
      writer.uint32(50).string(message.packageName);
    }
    if (message.versionCode !== 0) {
      writer.uint32(56).int32(message.versionCode);
    }
    if (message.minSdkVersion !== 0) {
      writer.uint32(64).int32(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      writer.uint32(72).int32(message.targetSdkVersion);
    }
    if (message.downloadToken !== "") {
      writer.uint32(82).string(message.downloadToken);
    }
    if (message.downloadUrl !== "") {
      writer.uint32(90).string(message.downloadUrl);
    }
    if (message.createdAt !== "") {
      writer.uint32(98).string(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadCompleteResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUploadCompleteResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.fileName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.fileSize = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.packageName = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.versionCode = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.minSdkVersion = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.targetSdkVersion = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.downloadToken = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.downloadUrl = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UploadCompleteResponse {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      fileName: isSet(object.fileName)
        ? globalThis.String(object.fileName)
        : isSet(object.file_name)
        ? globalThis.String(object.file_name)
        : "",
      fileSize: isSet(object.fileSize)
        ? globalThis.Number(object.fileSize)
        : isSet(object.file_size)
        ? globalThis.Number(object.file_size)
        : 0,
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      packageName: isSet(object.packageName)
        ? globalThis.String(object.packageName)
        : isSet(object.package_name)
        ? globalThis.String(object.package_name)
        : "",
      versionCode: isSet(object.versionCode)
        ? globalThis.Number(object.versionCode)
        : isSet(object.version_code)
        ? globalThis.Number(object.version_code)
        : 0,
      minSdkVersion: isSet(object.minSdkVersion)
        ? globalThis.Number(object.minSdkVersion)
        : isSet(object.min_sdk_version)
        ? globalThis.Number(object.min_sdk_version)
        : 0,
      targetSdkVersion: isSet(object.targetSdkVersion)
        ? globalThis.Number(object.targetSdkVersion)
        : isSet(object.target_sdk_version)
        ? globalThis.Number(object.target_sdk_version)
        : 0,
      downloadToken: isSet(object.downloadToken)
        ? globalThis.String(object.downloadToken)
        : isSet(object.download_token)
        ? globalThis.String(object.download_token)
        : "",
      downloadUrl: isSet(object.downloadUrl)
        ? globalThis.String(object.downloadUrl)
        : isSet(object.download_url)
        ? globalThis.String(object.download_url)
        : "",
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
    };
  },

  toJSON(message: UploadCompleteResponse): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.fileName !== "") {
      obj.fileName = message.fileName;
    }
    if (message.fileSize !== 0) {
      obj.fileSize = Math.round(message.fileSize);
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.packageName !== "") {
      obj.packageName = message.packageName;
    }
    if (message.versionCode !== 0) {
      obj.versionCode = Math.round(message.versionCode);
    }
    if (message.minSdkVersion !== 0) {
      obj.minSdkVersion = Math.round(message.minSdkVersion);
    }
    if (message.targetSdkVersion !== 0) {
      obj.targetSdkVersion = Math.round(message.targetSdkVersion);
    }
    if (message.downloadToken !== "") {
      obj.downloadToken = message.downloadToken;
    }
    if (message.downloadUrl !== "") {
      obj.downloadUrl = message.downloadUrl;
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    return obj;
  },

  create(base?: DeepPartial<UploadCompleteResponse>): UploadCompleteResponse {
    return UploadCompleteResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UploadCompleteResponse>): UploadCompleteResponse {
    const message = createBaseUploadCompleteResponse();
    message.id = object.id ?? 0;
    message.version = object.version ?? "";
    message.fileName = object.fileName ?? "";
    message.fileSize = object.fileSize ?? 0;
    message.description = object.description ?? "";
    message.packageName = object.packageName ?? "";
    message.versionCode = object.versionCode ?? 0;
    message.minSdkVersion = object.minSdkVersion ?? 0;
    message.targetSdkVersion = object.targetSdkVersion ?? 0;
    message.downloadToken = object.downloadToken ?? "";
    message.downloadUrl = object.downloadUrl ?? "";
    message.createdAt = object.createdAt ?? "";
    return message;
  },
};

function createBasePushPayload(): PushPayload {
  return { category: 0, title: "", body: "", imageUrl: "", deepLink: "", priority: 0, ttlSeconds: 0, data: {} };
}

export const PushPayload: MessageFns<PushPayload> = {
  encode(message: PushPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.category !== 0) {
      writer.uint32(8).int32(message.category);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.body !== "") {
      writer.uint32(26).string(message.body);
    }
    if (message.imageUrl !== "") {
      writer.uint32(34).string(message.imageUrl);
    }
    if (message.deepLink !== "") {
      writer.uint32(42).string(message.deepLink);
    }
    if (message.priority !== 0) {
      writer.uint32(48).int32(message.priority);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(56).int32(message.ttlSeconds);
    }
    globalThis.Object.entries(message.data).forEach(([key, value]: [string, string]) => {
      PushPayload_DataEntry.encode({ key: key as any, value }, writer.uint32(66).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushPayload();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.imageUrl = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deepLink = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.ttlSeconds = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          const entry8 = PushPayload_DataEntry.decode(reader, reader.uint32());
          if (entry8.value !== undefined) {
            message.data[entry8.key] = entry8.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushPayload {
    return {
      category: isSet(object.category) ? pushCategoryFromJSON(object.category) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      deepLink: isSet(object.deepLink)
        ? globalThis.String(object.deepLink)
        : isSet(object.deep_link)
        ? globalThis.String(object.deep_link)
        : "",
      priority: isSet(object.priority) ? pushPriorityFromJSON(object.priority) : 0,
      ttlSeconds: isSet(object.ttlSeconds)
        ? globalThis.Number(object.ttlSeconds)
        : isSet(object.ttl_seconds)
        ? globalThis.Number(object.ttl_seconds)
        : 0,
      data: isObject(object.data)
        ? (globalThis.Object.entries(object.data) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: PushPayload): unknown {
    const obj: any = {};
    if (message.category !== 0) {
      obj.category = pushCategoryToJSON(message.category);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.deepLink !== "") {
      obj.deepLink = message.deepLink;
    }
    if (message.priority !== 0) {
      obj.priority = pushPriorityToJSON(message.priority);
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    if (message.data) {
      const entries = globalThis.Object.entries(message.data) as [string, string][];
      if (entries.length > 0) {
        obj.data = {};
        entries.forEach(([k, v]) => {
          obj.data[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<PushPayload>): PushPayload {
    return PushPayload.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushPayload>): PushPayload {
    const message = createBasePushPayload();
    message.category = object.category ?? 0;
    message.title = object.title ?? "";
    message.body = object.body ?? "";
    message.imageUrl = object.imageUrl ?? "";
    message.deepLink = object.deepLink ?? "";
    message.priority = object.priority ?? 0;
    message.ttlSeconds = object.ttlSeconds ?? 0;
    message.data = (globalThis.Object.entries(object.data ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBasePushPayload_DataEntry(): PushPayload_DataEntry {
  return { key: "", value: "" };
}

export const PushPayload_DataEntry: MessageFns<PushPayload_DataEntry> = {
  encode(message: PushPayload_DataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushPayload_DataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushPayload_DataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushPayload_DataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PushPayload_DataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<PushPayload_DataEntry>): PushPayload_DataEntry {
    return PushPayload_DataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushPayload_DataEntry>): PushPayload_DataEntry {
    const message = createBasePushPayload_DataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateImmediatePushRequest(): CreateImmediatePushRequest {
  return { domainIds: [], payload: undefined };
}

export const CreateImmediatePushRequest: MessageFns<CreateImmediatePushRequest> = {
  encode(message: CreateImmediatePushRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.domainIds) {
      writer.uint32(v);
    }
    writer.join();
    if (message.payload !== undefined) {
      PushPayload.encode(message.payload, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateImmediatePushRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateImmediatePushRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.domainIds.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.domainIds.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = PushPayload.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateImmediatePushRequest {
    return {
      domainIds: globalThis.Array.isArray(object?.domainIds)
        ? object.domainIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.domain_ids)
        ? object.domain_ids.map((e: any) => globalThis.Number(e))
        : [],
      payload: isSet(object.payload) ? PushPayload.fromJSON(object.payload) : undefined,
    };
  },

  toJSON(message: CreateImmediatePushRequest): unknown {
    const obj: any = {};
    if (message.domainIds?.length) {
      obj.domainIds = message.domainIds.map((e) => Math.round(e));
    }
    if (message.payload !== undefined) {
      obj.payload = PushPayload.toJSON(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateImmediatePushRequest>): CreateImmediatePushRequest {
    return CreateImmediatePushRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateImmediatePushRequest>): CreateImmediatePushRequest {
    const message = createBaseCreateImmediatePushRequest();
    message.domainIds = object.domainIds?.map((e) => e) || [];
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? PushPayload.fromPartial(object.payload)
      : undefined;
    return message;
  },
};

function createBaseCreateScheduleRequest(): CreateScheduleRequest {
  return { domainIds: [], payload: undefined, scheduleType: 0, runAt: "", cronExpr: "" };
}

export const CreateScheduleRequest: MessageFns<CreateScheduleRequest> = {
  encode(message: CreateScheduleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.domainIds) {
      writer.uint32(v);
    }
    writer.join();
    if (message.payload !== undefined) {
      PushPayload.encode(message.payload, writer.uint32(18).fork()).join();
    }
    if (message.scheduleType !== 0) {
      writer.uint32(24).int32(message.scheduleType);
    }
    if (message.runAt !== "") {
      writer.uint32(34).string(message.runAt);
    }
    if (message.cronExpr !== "") {
      writer.uint32(42).string(message.cronExpr);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateScheduleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateScheduleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.domainIds.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.domainIds.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = PushPayload.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.scheduleType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.runAt = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cronExpr = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateScheduleRequest {
    return {
      domainIds: globalThis.Array.isArray(object?.domainIds)
        ? object.domainIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.domain_ids)
        ? object.domain_ids.map((e: any) => globalThis.Number(e))
        : [],
      payload: isSet(object.payload) ? PushPayload.fromJSON(object.payload) : undefined,
      scheduleType: isSet(object.scheduleType)
        ? pushScheduleTypeFromJSON(object.scheduleType)
        : isSet(object.schedule_type)
        ? pushScheduleTypeFromJSON(object.schedule_type)
        : 0,
      runAt: isSet(object.runAt)
        ? globalThis.String(object.runAt)
        : isSet(object.run_at)
        ? globalThis.String(object.run_at)
        : "",
      cronExpr: isSet(object.cronExpr)
        ? globalThis.String(object.cronExpr)
        : isSet(object.cron_expr)
        ? globalThis.String(object.cron_expr)
        : "",
    };
  },

  toJSON(message: CreateScheduleRequest): unknown {
    const obj: any = {};
    if (message.domainIds?.length) {
      obj.domainIds = message.domainIds.map((e) => Math.round(e));
    }
    if (message.payload !== undefined) {
      obj.payload = PushPayload.toJSON(message.payload);
    }
    if (message.scheduleType !== 0) {
      obj.scheduleType = pushScheduleTypeToJSON(message.scheduleType);
    }
    if (message.runAt !== "") {
      obj.runAt = message.runAt;
    }
    if (message.cronExpr !== "") {
      obj.cronExpr = message.cronExpr;
    }
    return obj;
  },

  create(base?: DeepPartial<CreateScheduleRequest>): CreateScheduleRequest {
    return CreateScheduleRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateScheduleRequest>): CreateScheduleRequest {
    const message = createBaseCreateScheduleRequest();
    message.domainIds = object.domainIds?.map((e) => e) || [];
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? PushPayload.fromPartial(object.payload)
      : undefined;
    message.scheduleType = object.scheduleType ?? 0;
    message.runAt = object.runAt ?? "";
    message.cronExpr = object.cronExpr ?? "";
    return message;
  },
};

function createBaseUpdateScheduleRequest(): UpdateScheduleRequest {
  return { id: 0, isActive: false, payload: undefined };
}

export const UpdateScheduleRequest: MessageFns<UpdateScheduleRequest> = {
  encode(message: UpdateScheduleRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.isActive !== false) {
      writer.uint32(16).bool(message.isActive);
    }
    if (message.payload !== undefined) {
      PushPayload.encode(message.payload, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateScheduleRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateScheduleRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.payload = PushPayload.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateScheduleRequest {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      isActive: isSet(object.isActive)
        ? globalThis.Boolean(object.isActive)
        : isSet(object.is_active)
        ? globalThis.Boolean(object.is_active)
        : false,
      payload: isSet(object.payload) ? PushPayload.fromJSON(object.payload) : undefined,
    };
  },

  toJSON(message: UpdateScheduleRequest): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.payload !== undefined) {
      obj.payload = PushPayload.toJSON(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<UpdateScheduleRequest>): UpdateScheduleRequest {
    return UpdateScheduleRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<UpdateScheduleRequest>): UpdateScheduleRequest {
    const message = createBaseUpdateScheduleRequest();
    message.id = object.id ?? 0;
    message.isActive = object.isActive ?? false;
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? PushPayload.fromPartial(object.payload)
      : undefined;
    return message;
  },
};

function createBaseScheduledPush(): ScheduledPush {
  return {
    id: 0,
    createdAt: "",
    updatedAt: "",
    userId: 0,
    scheduleType: 0,
    runAt: "",
    cronExpr: "",
    nextFireAt: "",
    lastFiredAt: "",
    isActive: false,
    domainIds: [],
    payload: undefined,
  };
}

export const ScheduledPush: MessageFns<ScheduledPush> = {
  encode(message: ScheduledPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.updatedAt !== "") {
      writer.uint32(26).string(message.updatedAt);
    }
    if (message.userId !== 0) {
      writer.uint32(32).uint32(message.userId);
    }
    if (message.scheduleType !== 0) {
      writer.uint32(40).int32(message.scheduleType);
    }
    if (message.runAt !== "") {
      writer.uint32(50).string(message.runAt);
    }
    if (message.cronExpr !== "") {
      writer.uint32(58).string(message.cronExpr);
    }
    if (message.nextFireAt !== "") {
      writer.uint32(66).string(message.nextFireAt);
    }
    if (message.lastFiredAt !== "") {
      writer.uint32(74).string(message.lastFiredAt);
    }
    if (message.isActive !== false) {
      writer.uint32(80).bool(message.isActive);
    }
    writer.uint32(90).fork();
    for (const v of message.domainIds) {
      writer.uint32(v);
    }
    writer.join();
    if (message.payload !== undefined) {
      PushPayload.encode(message.payload, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduledPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduledPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.userId = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.scheduleType = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.runAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.cronExpr = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.nextFireAt = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.lastFiredAt = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 11: {
          if (tag === 88) {
            message.domainIds.push(reader.uint32());

            continue;
          }

          if (tag === 90) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.domainIds.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.payload = PushPayload.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduledPush {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      updatedAt: isSet(object.updatedAt)
        ? globalThis.String(object.updatedAt)
        : isSet(object.updated_at)
        ? globalThis.String(object.updated_at)
        : "",
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      scheduleType: isSet(object.scheduleType)
        ? pushScheduleTypeFromJSON(object.scheduleType)
        : isSet(object.schedule_type)
        ? pushScheduleTypeFromJSON(object.schedule_type)
        : 0,
      runAt: isSet(object.runAt)
        ? globalThis.String(object.runAt)
        : isSet(object.run_at)
        ? globalThis.String(object.run_at)
        : "",
      cronExpr: isSet(object.cronExpr)
        ? globalThis.String(object.cronExpr)
        : isSet(object.cron_expr)
        ? globalThis.String(object.cron_expr)
        : "",
      nextFireAt: isSet(object.nextFireAt)
        ? globalThis.String(object.nextFireAt)
        : isSet(object.next_fire_at)
        ? globalThis.String(object.next_fire_at)
        : "",
      lastFiredAt: isSet(object.lastFiredAt)
        ? globalThis.String(object.lastFiredAt)
        : isSet(object.last_fired_at)
        ? globalThis.String(object.last_fired_at)
        : "",
      isActive: isSet(object.isActive)
        ? globalThis.Boolean(object.isActive)
        : isSet(object.is_active)
        ? globalThis.Boolean(object.is_active)
        : false,
      domainIds: globalThis.Array.isArray(object?.domainIds)
        ? object.domainIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.domain_ids)
        ? object.domain_ids.map((e: any) => globalThis.Number(e))
        : [],
      payload: isSet(object.payload) ? PushPayload.fromJSON(object.payload) : undefined,
    };
  },

  toJSON(message: ScheduledPush): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.updatedAt !== "") {
      obj.updatedAt = message.updatedAt;
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.scheduleType !== 0) {
      obj.scheduleType = pushScheduleTypeToJSON(message.scheduleType);
    }
    if (message.runAt !== "") {
      obj.runAt = message.runAt;
    }
    if (message.cronExpr !== "") {
      obj.cronExpr = message.cronExpr;
    }
    if (message.nextFireAt !== "") {
      obj.nextFireAt = message.nextFireAt;
    }
    if (message.lastFiredAt !== "") {
      obj.lastFiredAt = message.lastFiredAt;
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.domainIds?.length) {
      obj.domainIds = message.domainIds.map((e) => Math.round(e));
    }
    if (message.payload !== undefined) {
      obj.payload = PushPayload.toJSON(message.payload);
    }
    return obj;
  },

  create(base?: DeepPartial<ScheduledPush>): ScheduledPush {
    return ScheduledPush.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScheduledPush>): ScheduledPush {
    const message = createBaseScheduledPush();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.updatedAt = object.updatedAt ?? "";
    message.userId = object.userId ?? 0;
    message.scheduleType = object.scheduleType ?? 0;
    message.runAt = object.runAt ?? "";
    message.cronExpr = object.cronExpr ?? "";
    message.nextFireAt = object.nextFireAt ?? "";
    message.lastFiredAt = object.lastFiredAt ?? "";
    message.isActive = object.isActive ?? false;
    message.domainIds = object.domainIds?.map((e) => e) || [];
    message.payload = (object.payload !== undefined && object.payload !== null)
      ? PushPayload.fromPartial(object.payload)
      : undefined;
    return message;
  },
};

function createBaseScheduledPushPage(): ScheduledPushPage {
  return { data: [], page: undefined };
}

export const ScheduledPushPage: MessageFns<ScheduledPushPage> = {
  encode(message: ScheduledPushPage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      ScheduledPush.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ScheduledPushPage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseScheduledPushPage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(ScheduledPush.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ScheduledPushPage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => ScheduledPush.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ScheduledPushPage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => ScheduledPush.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<ScheduledPushPage>): ScheduledPushPage {
    return ScheduledPushPage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ScheduledPushPage>): ScheduledPushPage {
    const message = createBaseScheduledPushPage();
    message.data = object.data?.map((e) => ScheduledPush.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBasePushMessage(): PushMessage {
  return {
    id: 0,
    createdAt: "",
    userId: 0,
    category: 0,
    title: "",
    body: "",
    imageUrl: "",
    deepLink: "",
    priority: 0,
    ttlSeconds: 0,
    data: {},
    source: "",
    scheduledId: 0,
    domainIds: [],
  };
}

export const PushMessage: MessageFns<PushMessage> = {
  encode(message: PushMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createdAt !== "") {
      writer.uint32(18).string(message.createdAt);
    }
    if (message.userId !== 0) {
      writer.uint32(24).uint32(message.userId);
    }
    if (message.category !== 0) {
      writer.uint32(32).int32(message.category);
    }
    if (message.title !== "") {
      writer.uint32(42).string(message.title);
    }
    if (message.body !== "") {
      writer.uint32(50).string(message.body);
    }
    if (message.imageUrl !== "") {
      writer.uint32(58).string(message.imageUrl);
    }
    if (message.deepLink !== "") {
      writer.uint32(66).string(message.deepLink);
    }
    if (message.priority !== 0) {
      writer.uint32(72).int32(message.priority);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(80).int32(message.ttlSeconds);
    }
    globalThis.Object.entries(message.data).forEach(([key, value]: [string, string]) => {
      PushMessage_DataEntry.encode({ key: key as any, value }, writer.uint32(90).fork()).join();
    });
    if (message.source !== "") {
      writer.uint32(98).string(message.source);
    }
    if (message.scheduledId !== 0) {
      writer.uint32(104).uint32(message.scheduledId);
    }
    writer.uint32(114).fork();
    for (const v of message.domainIds) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.createdAt = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.userId = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.imageUrl = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.deepLink = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.ttlSeconds = reader.int32();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          const entry11 = PushMessage_DataEntry.decode(reader, reader.uint32());
          if (entry11.value !== undefined) {
            message.data[entry11.key] = entry11.value;
          }
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.source = reader.string();
          continue;
        }
        case 13: {
          if (tag !== 104) {
            break;
          }

          message.scheduledId = reader.uint32();
          continue;
        }
        case 14: {
          if (tag === 112) {
            message.domainIds.push(reader.uint32());

            continue;
          }

          if (tag === 114) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.domainIds.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushMessage {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createdAt: isSet(object.createdAt)
        ? globalThis.String(object.createdAt)
        : isSet(object.created_at)
        ? globalThis.String(object.created_at)
        : "",
      userId: isSet(object.userId)
        ? globalThis.Number(object.userId)
        : isSet(object.user_id)
        ? globalThis.Number(object.user_id)
        : 0,
      category: isSet(object.category) ? pushCategoryFromJSON(object.category) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      deepLink: isSet(object.deepLink)
        ? globalThis.String(object.deepLink)
        : isSet(object.deep_link)
        ? globalThis.String(object.deep_link)
        : "",
      priority: isSet(object.priority) ? pushPriorityFromJSON(object.priority) : 0,
      ttlSeconds: isSet(object.ttlSeconds)
        ? globalThis.Number(object.ttlSeconds)
        : isSet(object.ttl_seconds)
        ? globalThis.Number(object.ttl_seconds)
        : 0,
      data: isObject(object.data)
        ? (globalThis.Object.entries(object.data) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      scheduledId: isSet(object.scheduledId)
        ? globalThis.Number(object.scheduledId)
        : isSet(object.scheduled_id)
        ? globalThis.Number(object.scheduled_id)
        : 0,
      domainIds: globalThis.Array.isArray(object?.domainIds)
        ? object.domainIds.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.domain_ids)
        ? object.domain_ids.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: PushMessage): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createdAt !== "") {
      obj.createdAt = message.createdAt;
    }
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.category !== 0) {
      obj.category = pushCategoryToJSON(message.category);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.deepLink !== "") {
      obj.deepLink = message.deepLink;
    }
    if (message.priority !== 0) {
      obj.priority = pushPriorityToJSON(message.priority);
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    if (message.data) {
      const entries = globalThis.Object.entries(message.data) as [string, string][];
      if (entries.length > 0) {
        obj.data = {};
        entries.forEach(([k, v]) => {
          obj.data[k] = v;
        });
      }
    }
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.scheduledId !== 0) {
      obj.scheduledId = Math.round(message.scheduledId);
    }
    if (message.domainIds?.length) {
      obj.domainIds = message.domainIds.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PushMessage>): PushMessage {
    return PushMessage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushMessage>): PushMessage {
    const message = createBasePushMessage();
    message.id = object.id ?? 0;
    message.createdAt = object.createdAt ?? "";
    message.userId = object.userId ?? 0;
    message.category = object.category ?? 0;
    message.title = object.title ?? "";
    message.body = object.body ?? "";
    message.imageUrl = object.imageUrl ?? "";
    message.deepLink = object.deepLink ?? "";
    message.priority = object.priority ?? 0;
    message.ttlSeconds = object.ttlSeconds ?? 0;
    message.data = (globalThis.Object.entries(object.data ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.source = object.source ?? "";
    message.scheduledId = object.scheduledId ?? 0;
    message.domainIds = object.domainIds?.map((e) => e) || [];
    return message;
  },
};

function createBasePushMessage_DataEntry(): PushMessage_DataEntry {
  return { key: "", value: "" };
}

export const PushMessage_DataEntry: MessageFns<PushMessage_DataEntry> = {
  encode(message: PushMessage_DataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushMessage_DataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushMessage_DataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushMessage_DataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: PushMessage_DataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<PushMessage_DataEntry>): PushMessage_DataEntry {
    return PushMessage_DataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushMessage_DataEntry>): PushMessage_DataEntry {
    const message = createBasePushMessage_DataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBasePushMessagePage(): PushMessagePage {
  return { data: [], page: undefined };
}

export const PushMessagePage: MessageFns<PushMessagePage> = {
  encode(message: PushMessagePage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.data) {
      PushMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      CursorPageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushMessagePage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushMessagePage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.data.push(PushMessage.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = CursorPageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushMessagePage {
    return {
      data: globalThis.Array.isArray(object?.data) ? object.data.map((e: any) => PushMessage.fromJSON(e)) : [],
      page: isSet(object.page) ? CursorPageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: PushMessagePage): unknown {
    const obj: any = {};
    if (message.data?.length) {
      obj.data = message.data.map((e) => PushMessage.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = CursorPageInfo.toJSON(message.page);
    }
    return obj;
  },

  create(base?: DeepPartial<PushMessagePage>): PushMessagePage {
    return PushMessagePage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushMessagePage>): PushMessagePage {
    const message = createBasePushMessagePage();
    message.data = object.data?.map((e) => PushMessage.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null)
      ? CursorPageInfo.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseFailureBreakdown(): FailureBreakdown {
  return { reason: 0, count: 0 };
}

export const FailureBreakdown: MessageFns<FailureBreakdown> = {
  encode(message: FailureBreakdown, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reason !== 0) {
      writer.uint32(8).int32(message.reason);
    }
    if (message.count !== 0) {
      writer.uint32(16).int64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FailureBreakdown {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFailureBreakdown();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FailureBreakdown {
    return {
      reason: isSet(object.reason) ? deliveryFailureReasonFromJSON(object.reason) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: FailureBreakdown): unknown {
    const obj: any = {};
    if (message.reason !== 0) {
      obj.reason = deliveryFailureReasonToJSON(message.reason);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<FailureBreakdown>): FailureBreakdown {
    return FailureBreakdown.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<FailureBreakdown>): FailureBreakdown {
    const message = createBaseFailureBreakdown();
    message.reason = object.reason ?? 0;
    message.count = object.count ?? 0;
    return message;
  },
};

function createBasePushMetricsByCampaign(): PushMetricsByCampaign {
  return {
    pushId: 0,
    targetsTotal: 0,
    delivered: 0,
    notDelivered: 0,
    deliveryRate: 0,
    latencyP50Ms: 0,
    latencyP95Ms: 0,
    failuresByReason: [],
  };
}

export const PushMetricsByCampaign: MessageFns<PushMetricsByCampaign> = {
  encode(message: PushMetricsByCampaign, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pushId !== 0) {
      writer.uint32(8).uint32(message.pushId);
    }
    if (message.targetsTotal !== 0) {
      writer.uint32(16).int64(message.targetsTotal);
    }
    if (message.delivered !== 0) {
      writer.uint32(24).int64(message.delivered);
    }
    if (message.notDelivered !== 0) {
      writer.uint32(32).int64(message.notDelivered);
    }
    if (message.deliveryRate !== 0) {
      writer.uint32(41).double(message.deliveryRate);
    }
    if (message.latencyP50Ms !== 0) {
      writer.uint32(48).int64(message.latencyP50Ms);
    }
    if (message.latencyP95Ms !== 0) {
      writer.uint32(56).int64(message.latencyP95Ms);
    }
    for (const v of message.failuresByReason) {
      FailureBreakdown.encode(v!, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushMetricsByCampaign {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushMetricsByCampaign();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pushId = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.targetsTotal = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.delivered = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.notDelivered = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 41) {
            break;
          }

          message.deliveryRate = reader.double();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.latencyP50Ms = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.latencyP95Ms = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.failuresByReason.push(FailureBreakdown.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushMetricsByCampaign {
    return {
      pushId: isSet(object.pushId)
        ? globalThis.Number(object.pushId)
        : isSet(object.push_id)
        ? globalThis.Number(object.push_id)
        : 0,
      targetsTotal: isSet(object.targetsTotal)
        ? globalThis.Number(object.targetsTotal)
        : isSet(object.targets_total)
        ? globalThis.Number(object.targets_total)
        : 0,
      delivered: isSet(object.delivered) ? globalThis.Number(object.delivered) : 0,
      notDelivered: isSet(object.notDelivered)
        ? globalThis.Number(object.notDelivered)
        : isSet(object.not_delivered)
        ? globalThis.Number(object.not_delivered)
        : 0,
      deliveryRate: isSet(object.deliveryRate)
        ? globalThis.Number(object.deliveryRate)
        : isSet(object.delivery_rate)
        ? globalThis.Number(object.delivery_rate)
        : 0,
      latencyP50Ms: isSet(object.latencyP50Ms)
        ? globalThis.Number(object.latencyP50Ms)
        : isSet(object.latency_p50_ms)
        ? globalThis.Number(object.latency_p50_ms)
        : 0,
      latencyP95Ms: isSet(object.latencyP95Ms)
        ? globalThis.Number(object.latencyP95Ms)
        : isSet(object.latency_p95_ms)
        ? globalThis.Number(object.latency_p95_ms)
        : 0,
      failuresByReason: globalThis.Array.isArray(object?.failuresByReason)
        ? object.failuresByReason.map((e: any) => FailureBreakdown.fromJSON(e))
        : globalThis.Array.isArray(object?.failures_by_reason)
        ? object.failures_by_reason.map((e: any) => FailureBreakdown.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PushMetricsByCampaign): unknown {
    const obj: any = {};
    if (message.pushId !== 0) {
      obj.pushId = Math.round(message.pushId);
    }
    if (message.targetsTotal !== 0) {
      obj.targetsTotal = Math.round(message.targetsTotal);
    }
    if (message.delivered !== 0) {
      obj.delivered = Math.round(message.delivered);
    }
    if (message.notDelivered !== 0) {
      obj.notDelivered = Math.round(message.notDelivered);
    }
    if (message.deliveryRate !== 0) {
      obj.deliveryRate = message.deliveryRate;
    }
    if (message.latencyP50Ms !== 0) {
      obj.latencyP50Ms = Math.round(message.latencyP50Ms);
    }
    if (message.latencyP95Ms !== 0) {
      obj.latencyP95Ms = Math.round(message.latencyP95Ms);
    }
    if (message.failuresByReason?.length) {
      obj.failuresByReason = message.failuresByReason.map((e) => FailureBreakdown.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<PushMetricsByCampaign>): PushMetricsByCampaign {
    return PushMetricsByCampaign.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushMetricsByCampaign>): PushMetricsByCampaign {
    const message = createBasePushMetricsByCampaign();
    message.pushId = object.pushId ?? 0;
    message.targetsTotal = object.targetsTotal ?? 0;
    message.delivered = object.delivered ?? 0;
    message.notDelivered = object.notDelivered ?? 0;
    message.deliveryRate = object.deliveryRate ?? 0;
    message.latencyP50Ms = object.latencyP50Ms ?? 0;
    message.latencyP95Ms = object.latencyP95Ms ?? 0;
    message.failuresByReason = object.failuresByReason?.map((e) => FailureBreakdown.fromPartial(e)) || [];
    return message;
  },
};

function createBasePlatformCount(): PlatformCount {
  return { platform: "", count: 0 };
}

export const PlatformCount: MessageFns<PlatformCount> = {
  encode(message: PlatformCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.platform !== "") {
      writer.uint32(10).string(message.platform);
    }
    if (message.count !== 0) {
      writer.uint32(16).int64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlatformCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlatformCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.platform = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlatformCount {
    return {
      platform: isSet(object.platform) ? globalThis.String(object.platform) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: PlatformCount): unknown {
    const obj: any = {};
    if (message.platform !== "") {
      obj.platform = message.platform;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<PlatformCount>): PlatformCount {
    return PlatformCount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PlatformCount>): PlatformCount {
    const message = createBasePlatformCount();
    message.platform = object.platform ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseAppVersionCount(): AppVersionCount {
  return { version: "", count: 0 };
}

export const AppVersionCount: MessageFns<AppVersionCount> = {
  encode(message: AppVersionCount, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== "") {
      writer.uint32(10).string(message.version);
    }
    if (message.count !== 0) {
      writer.uint32(16).int64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AppVersionCount {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAppVersionCount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.version = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AppVersionCount {
    return {
      version: isSet(object.version) ? globalThis.String(object.version) : "",
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: AppVersionCount): unknown {
    const obj: any = {};
    if (message.version !== "") {
      obj.version = message.version;
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<AppVersionCount>): AppVersionCount {
    return AppVersionCount.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AppVersionCount>): AppVersionCount {
    const message = createBaseAppVersionCount();
    message.version = object.version ?? "";
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseHourBucket(): HourBucket {
  return { hour: 0, count: 0 };
}

export const HourBucket: MessageFns<HourBucket> = {
  encode(message: HourBucket, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hour !== 0) {
      writer.uint32(8).int32(message.hour);
    }
    if (message.count !== 0) {
      writer.uint32(16).int64(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HourBucket {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHourBucket();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.hour = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): HourBucket {
    return {
      hour: isSet(object.hour) ? globalThis.Number(object.hour) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: HourBucket): unknown {
    const obj: any = {};
    if (message.hour !== 0) {
      obj.hour = Math.round(message.hour);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create(base?: DeepPartial<HourBucket>): HourBucket {
    return HourBucket.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<HourBucket>): HourBucket {
    const message = createBaseHourBucket();
    message.hour = object.hour ?? 0;
    message.count = object.count ?? 0;
    return message;
  },
};

function createBasePushMetricsAggregate(): PushMetricsAggregate {
  return {
    messagesSentTotal: 0,
    messagesDeliveredTotal: 0,
    deliveryRateTotal: 0,
    activeSchedules: 0,
    scheduledFires24h: 0,
    scheduledFires7d: 0,
    scheduledFires30d: 0,
    scheduledFailures24h: 0,
    scheduledFailures7d: 0,
    scheduledFailures30d: 0,
    audienceSize: 0,
    audiencePeakToday: 0,
    topPlatforms: [],
    topAppVersions: [],
    hourlyHistogram30d: [],
    lastPushAt: "",
  };
}

export const PushMetricsAggregate: MessageFns<PushMetricsAggregate> = {
  encode(message: PushMetricsAggregate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messagesSentTotal !== 0) {
      writer.uint32(8).int64(message.messagesSentTotal);
    }
    if (message.messagesDeliveredTotal !== 0) {
      writer.uint32(16).int64(message.messagesDeliveredTotal);
    }
    if (message.deliveryRateTotal !== 0) {
      writer.uint32(25).double(message.deliveryRateTotal);
    }
    if (message.activeSchedules !== 0) {
      writer.uint32(32).int64(message.activeSchedules);
    }
    if (message.scheduledFires24h !== 0) {
      writer.uint32(40).int64(message.scheduledFires24h);
    }
    if (message.scheduledFires7d !== 0) {
      writer.uint32(48).int64(message.scheduledFires7d);
    }
    if (message.scheduledFires30d !== 0) {
      writer.uint32(56).int64(message.scheduledFires30d);
    }
    if (message.scheduledFailures24h !== 0) {
      writer.uint32(64).int64(message.scheduledFailures24h);
    }
    if (message.scheduledFailures7d !== 0) {
      writer.uint32(72).int64(message.scheduledFailures7d);
    }
    if (message.scheduledFailures30d !== 0) {
      writer.uint32(80).int64(message.scheduledFailures30d);
    }
    if (message.audienceSize !== 0) {
      writer.uint32(88).int64(message.audienceSize);
    }
    if (message.audiencePeakToday !== 0) {
      writer.uint32(96).int64(message.audiencePeakToday);
    }
    for (const v of message.topPlatforms) {
      PlatformCount.encode(v!, writer.uint32(106).fork()).join();
    }
    for (const v of message.topAppVersions) {
      AppVersionCount.encode(v!, writer.uint32(114).fork()).join();
    }
    for (const v of message.hourlyHistogram30d) {
      HourBucket.encode(v!, writer.uint32(122).fork()).join();
    }
    if (message.lastPushAt !== "") {
      writer.uint32(130).string(message.lastPushAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PushMetricsAggregate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePushMetricsAggregate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.messagesSentTotal = longToNumber(reader.int64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.messagesDeliveredTotal = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.deliveryRateTotal = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.activeSchedules = longToNumber(reader.int64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.scheduledFires24h = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.scheduledFires7d = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.scheduledFires30d = longToNumber(reader.int64());
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.scheduledFailures24h = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.scheduledFailures7d = longToNumber(reader.int64());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.scheduledFailures30d = longToNumber(reader.int64());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.audienceSize = longToNumber(reader.int64());
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.audiencePeakToday = longToNumber(reader.int64());
          continue;
        }
        case 13: {
          if (tag !== 106) {
            break;
          }

          message.topPlatforms.push(PlatformCount.decode(reader, reader.uint32()));
          continue;
        }
        case 14: {
          if (tag !== 114) {
            break;
          }

          message.topAppVersions.push(AppVersionCount.decode(reader, reader.uint32()));
          continue;
        }
        case 15: {
          if (tag !== 122) {
            break;
          }

          message.hourlyHistogram30d.push(HourBucket.decode(reader, reader.uint32()));
          continue;
        }
        case 16: {
          if (tag !== 130) {
            break;
          }

          message.lastPushAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PushMetricsAggregate {
    return {
      messagesSentTotal: isSet(object.messagesSentTotal)
        ? globalThis.Number(object.messagesSentTotal)
        : isSet(object.messages_sent_total)
        ? globalThis.Number(object.messages_sent_total)
        : 0,
      messagesDeliveredTotal: isSet(object.messagesDeliveredTotal)
        ? globalThis.Number(object.messagesDeliveredTotal)
        : isSet(object.messages_delivered_total)
        ? globalThis.Number(object.messages_delivered_total)
        : 0,
      deliveryRateTotal: isSet(object.deliveryRateTotal)
        ? globalThis.Number(object.deliveryRateTotal)
        : isSet(object.delivery_rate_total)
        ? globalThis.Number(object.delivery_rate_total)
        : 0,
      activeSchedules: isSet(object.activeSchedules)
        ? globalThis.Number(object.activeSchedules)
        : isSet(object.active_schedules)
        ? globalThis.Number(object.active_schedules)
        : 0,
      scheduledFires24h: isSet(object.scheduledFires24h)
        ? globalThis.Number(object.scheduledFires24h)
        : isSet(object.scheduled_fires_24h)
        ? globalThis.Number(object.scheduled_fires_24h)
        : 0,
      scheduledFires7d: isSet(object.scheduledFires7d)
        ? globalThis.Number(object.scheduledFires7d)
        : isSet(object.scheduled_fires_7d)
        ? globalThis.Number(object.scheduled_fires_7d)
        : 0,
      scheduledFires30d: isSet(object.scheduledFires30d)
        ? globalThis.Number(object.scheduledFires30d)
        : isSet(object.scheduled_fires_30d)
        ? globalThis.Number(object.scheduled_fires_30d)
        : 0,
      scheduledFailures24h: isSet(object.scheduledFailures24h)
        ? globalThis.Number(object.scheduledFailures24h)
        : isSet(object.scheduled_failures_24h)
        ? globalThis.Number(object.scheduled_failures_24h)
        : 0,
      scheduledFailures7d: isSet(object.scheduledFailures7d)
        ? globalThis.Number(object.scheduledFailures7d)
        : isSet(object.scheduled_failures_7d)
        ? globalThis.Number(object.scheduled_failures_7d)
        : 0,
      scheduledFailures30d: isSet(object.scheduledFailures30d)
        ? globalThis.Number(object.scheduledFailures30d)
        : isSet(object.scheduled_failures_30d)
        ? globalThis.Number(object.scheduled_failures_30d)
        : 0,
      audienceSize: isSet(object.audienceSize)
        ? globalThis.Number(object.audienceSize)
        : isSet(object.audience_size)
        ? globalThis.Number(object.audience_size)
        : 0,
      audiencePeakToday: isSet(object.audiencePeakToday)
        ? globalThis.Number(object.audiencePeakToday)
        : isSet(object.audience_peak_today)
        ? globalThis.Number(object.audience_peak_today)
        : 0,
      topPlatforms: globalThis.Array.isArray(object?.topPlatforms)
        ? object.topPlatforms.map((e: any) => PlatformCount.fromJSON(e))
        : globalThis.Array.isArray(object?.top_platforms)
        ? object.top_platforms.map((e: any) => PlatformCount.fromJSON(e))
        : [],
      topAppVersions: globalThis.Array.isArray(object?.topAppVersions)
        ? object.topAppVersions.map((e: any) => AppVersionCount.fromJSON(e))
        : globalThis.Array.isArray(object?.top_app_versions)
        ? object.top_app_versions.map((e: any) => AppVersionCount.fromJSON(e))
        : [],
      hourlyHistogram30d: globalThis.Array.isArray(object?.hourlyHistogram30d)
        ? object.hourlyHistogram30d.map((e: any) => HourBucket.fromJSON(e))
        : globalThis.Array.isArray(object?.hourly_histogram_30d)
        ? object.hourly_histogram_30d.map((e: any) => HourBucket.fromJSON(e))
        : [],
      lastPushAt: isSet(object.lastPushAt)
        ? globalThis.String(object.lastPushAt)
        : isSet(object.last_push_at)
        ? globalThis.String(object.last_push_at)
        : "",
    };
  },

  toJSON(message: PushMetricsAggregate): unknown {
    const obj: any = {};
    if (message.messagesSentTotal !== 0) {
      obj.messagesSentTotal = Math.round(message.messagesSentTotal);
    }
    if (message.messagesDeliveredTotal !== 0) {
      obj.messagesDeliveredTotal = Math.round(message.messagesDeliveredTotal);
    }
    if (message.deliveryRateTotal !== 0) {
      obj.deliveryRateTotal = message.deliveryRateTotal;
    }
    if (message.activeSchedules !== 0) {
      obj.activeSchedules = Math.round(message.activeSchedules);
    }
    if (message.scheduledFires24h !== 0) {
      obj.scheduledFires24h = Math.round(message.scheduledFires24h);
    }
    if (message.scheduledFires7d !== 0) {
      obj.scheduledFires7d = Math.round(message.scheduledFires7d);
    }
    if (message.scheduledFires30d !== 0) {
      obj.scheduledFires30d = Math.round(message.scheduledFires30d);
    }
    if (message.scheduledFailures24h !== 0) {
      obj.scheduledFailures24h = Math.round(message.scheduledFailures24h);
    }
    if (message.scheduledFailures7d !== 0) {
      obj.scheduledFailures7d = Math.round(message.scheduledFailures7d);
    }
    if (message.scheduledFailures30d !== 0) {
      obj.scheduledFailures30d = Math.round(message.scheduledFailures30d);
    }
    if (message.audienceSize !== 0) {
      obj.audienceSize = Math.round(message.audienceSize);
    }
    if (message.audiencePeakToday !== 0) {
      obj.audiencePeakToday = Math.round(message.audiencePeakToday);
    }
    if (message.topPlatforms?.length) {
      obj.topPlatforms = message.topPlatforms.map((e) => PlatformCount.toJSON(e));
    }
    if (message.topAppVersions?.length) {
      obj.topAppVersions = message.topAppVersions.map((e) => AppVersionCount.toJSON(e));
    }
    if (message.hourlyHistogram30d?.length) {
      obj.hourlyHistogram30d = message.hourlyHistogram30d.map((e) => HourBucket.toJSON(e));
    }
    if (message.lastPushAt !== "") {
      obj.lastPushAt = message.lastPushAt;
    }
    return obj;
  },

  create(base?: DeepPartial<PushMetricsAggregate>): PushMetricsAggregate {
    return PushMetricsAggregate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PushMetricsAggregate>): PushMetricsAggregate {
    const message = createBasePushMetricsAggregate();
    message.messagesSentTotal = object.messagesSentTotal ?? 0;
    message.messagesDeliveredTotal = object.messagesDeliveredTotal ?? 0;
    message.deliveryRateTotal = object.deliveryRateTotal ?? 0;
    message.activeSchedules = object.activeSchedules ?? 0;
    message.scheduledFires24h = object.scheduledFires24h ?? 0;
    message.scheduledFires7d = object.scheduledFires7d ?? 0;
    message.scheduledFires30d = object.scheduledFires30d ?? 0;
    message.scheduledFailures24h = object.scheduledFailures24h ?? 0;
    message.scheduledFailures7d = object.scheduledFailures7d ?? 0;
    message.scheduledFailures30d = object.scheduledFailures30d ?? 0;
    message.audienceSize = object.audienceSize ?? 0;
    message.audiencePeakToday = object.audiencePeakToday ?? 0;
    message.topPlatforms = object.topPlatforms?.map((e) => PlatformCount.fromPartial(e)) || [];
    message.topAppVersions = object.topAppVersions?.map((e) => AppVersionCount.fromPartial(e)) || [];
    message.hourlyHistogram30d = object.hourlyHistogram30d?.map((e) => HourBucket.fromPartial(e)) || [];
    message.lastPushAt = object.lastPushAt ?? "";
    return message;
  },
};

function createBaseWsFrame(): WsFrame {
  return { hello: undefined, push: undefined, pushAck: undefined, ping: undefined, pong: undefined, error: undefined };
}

export const WsFrame: MessageFns<WsFrame> = {
  encode(message: WsFrame, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hello !== undefined) {
      WsHello.encode(message.hello, writer.uint32(10).fork()).join();
    }
    if (message.push !== undefined) {
      WsPush.encode(message.push, writer.uint32(18).fork()).join();
    }
    if (message.pushAck !== undefined) {
      WsPushAck.encode(message.pushAck, writer.uint32(26).fork()).join();
    }
    if (message.ping !== undefined) {
      WsPing.encode(message.ping, writer.uint32(34).fork()).join();
    }
    if (message.pong !== undefined) {
      WsPong.encode(message.pong, writer.uint32(42).fork()).join();
    }
    if (message.error !== undefined) {
      WsError.encode(message.error, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsFrame {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsFrame();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hello = WsHello.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.push = WsPush.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pushAck = WsPushAck.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.ping = WsPing.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.pong = WsPong.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.error = WsError.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsFrame {
    return {
      hello: isSet(object.hello) ? WsHello.fromJSON(object.hello) : undefined,
      push: isSet(object.push) ? WsPush.fromJSON(object.push) : undefined,
      pushAck: isSet(object.pushAck)
        ? WsPushAck.fromJSON(object.pushAck)
        : isSet(object.push_ack)
        ? WsPushAck.fromJSON(object.push_ack)
        : undefined,
      ping: isSet(object.ping) ? WsPing.fromJSON(object.ping) : undefined,
      pong: isSet(object.pong) ? WsPong.fromJSON(object.pong) : undefined,
      error: isSet(object.error) ? WsError.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: WsFrame): unknown {
    const obj: any = {};
    if (message.hello !== undefined) {
      obj.hello = WsHello.toJSON(message.hello);
    }
    if (message.push !== undefined) {
      obj.push = WsPush.toJSON(message.push);
    }
    if (message.pushAck !== undefined) {
      obj.pushAck = WsPushAck.toJSON(message.pushAck);
    }
    if (message.ping !== undefined) {
      obj.ping = WsPing.toJSON(message.ping);
    }
    if (message.pong !== undefined) {
      obj.pong = WsPong.toJSON(message.pong);
    }
    if (message.error !== undefined) {
      obj.error = WsError.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<WsFrame>): WsFrame {
    return WsFrame.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsFrame>): WsFrame {
    const message = createBaseWsFrame();
    message.hello = (object.hello !== undefined && object.hello !== null)
      ? WsHello.fromPartial(object.hello)
      : undefined;
    message.push = (object.push !== undefined && object.push !== null) ? WsPush.fromPartial(object.push) : undefined;
    message.pushAck = (object.pushAck !== undefined && object.pushAck !== null)
      ? WsPushAck.fromPartial(object.pushAck)
      : undefined;
    message.ping = (object.ping !== undefined && object.ping !== null) ? WsPing.fromPartial(object.ping) : undefined;
    message.pong = (object.pong !== undefined && object.pong !== null) ? WsPong.fromPartial(object.pong) : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? WsError.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseWsHello(): WsHello {
  return { deviceId: 0, serverTime: 0 };
}

export const WsHello: MessageFns<WsHello> = {
  encode(message: WsHello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== 0) {
      writer.uint32(8).uint64(message.deviceId);
    }
    if (message.serverTime !== 0) {
      writer.uint32(16).int64(message.serverTime);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsHello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.deviceId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.serverTime = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsHello {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
      serverTime: isSet(object.serverTime)
        ? globalThis.Number(object.serverTime)
        : isSet(object.server_time)
        ? globalThis.Number(object.server_time)
        : 0,
    };
  },

  toJSON(message: WsHello): unknown {
    const obj: any = {};
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.serverTime !== 0) {
      obj.serverTime = Math.round(message.serverTime);
    }
    return obj;
  },

  create(base?: DeepPartial<WsHello>): WsHello {
    return WsHello.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsHello>): WsHello {
    const message = createBaseWsHello();
    message.deviceId = object.deviceId ?? 0;
    message.serverTime = object.serverTime ?? 0;
    return message;
  },
};

function createBaseWsPush(): WsPush {
  return {
    pushId: 0,
    messageId: 0,
    category: 0,
    title: "",
    body: "",
    imageUrl: "",
    deepLink: "",
    priority: 0,
    ttlSeconds: 0,
    data: {},
    sentAt: 0,
  };
}

export const WsPush: MessageFns<WsPush> = {
  encode(message: WsPush, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pushId !== 0) {
      writer.uint32(8).uint64(message.pushId);
    }
    if (message.messageId !== 0) {
      writer.uint32(16).uint64(message.messageId);
    }
    if (message.category !== 0) {
      writer.uint32(24).int32(message.category);
    }
    if (message.title !== "") {
      writer.uint32(34).string(message.title);
    }
    if (message.body !== "") {
      writer.uint32(42).string(message.body);
    }
    if (message.imageUrl !== "") {
      writer.uint32(50).string(message.imageUrl);
    }
    if (message.deepLink !== "") {
      writer.uint32(58).string(message.deepLink);
    }
    if (message.priority !== 0) {
      writer.uint32(64).int32(message.priority);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(72).int32(message.ttlSeconds);
    }
    globalThis.Object.entries(message.data).forEach(([key, value]: [string, string]) => {
      WsPush_DataEntry.encode({ key: key as any, value }, writer.uint32(82).fork()).join();
    });
    if (message.sentAt !== 0) {
      writer.uint32(88).int64(message.sentAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsPush {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsPush();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pushId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.messageId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.category = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.body = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.imageUrl = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.deepLink = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.priority = reader.int32() as any;
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.ttlSeconds = reader.int32();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          const entry10 = WsPush_DataEntry.decode(reader, reader.uint32());
          if (entry10.value !== undefined) {
            message.data[entry10.key] = entry10.value;
          }
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.sentAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsPush {
    return {
      pushId: isSet(object.pushId)
        ? globalThis.Number(object.pushId)
        : isSet(object.push_id)
        ? globalThis.Number(object.push_id)
        : 0,
      messageId: isSet(object.messageId)
        ? globalThis.Number(object.messageId)
        : isSet(object.message_id)
        ? globalThis.Number(object.message_id)
        : 0,
      category: isSet(object.category) ? pushCategoryFromJSON(object.category) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      body: isSet(object.body) ? globalThis.String(object.body) : "",
      imageUrl: isSet(object.imageUrl)
        ? globalThis.String(object.imageUrl)
        : isSet(object.image_url)
        ? globalThis.String(object.image_url)
        : "",
      deepLink: isSet(object.deepLink)
        ? globalThis.String(object.deepLink)
        : isSet(object.deep_link)
        ? globalThis.String(object.deep_link)
        : "",
      priority: isSet(object.priority) ? pushPriorityFromJSON(object.priority) : 0,
      ttlSeconds: isSet(object.ttlSeconds)
        ? globalThis.Number(object.ttlSeconds)
        : isSet(object.ttl_seconds)
        ? globalThis.Number(object.ttl_seconds)
        : 0,
      data: isObject(object.data)
        ? (globalThis.Object.entries(object.data) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
      sentAt: isSet(object.sentAt)
        ? globalThis.Number(object.sentAt)
        : isSet(object.sent_at)
        ? globalThis.Number(object.sent_at)
        : 0,
    };
  },

  toJSON(message: WsPush): unknown {
    const obj: any = {};
    if (message.pushId !== 0) {
      obj.pushId = Math.round(message.pushId);
    }
    if (message.messageId !== 0) {
      obj.messageId = Math.round(message.messageId);
    }
    if (message.category !== 0) {
      obj.category = pushCategoryToJSON(message.category);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.body !== "") {
      obj.body = message.body;
    }
    if (message.imageUrl !== "") {
      obj.imageUrl = message.imageUrl;
    }
    if (message.deepLink !== "") {
      obj.deepLink = message.deepLink;
    }
    if (message.priority !== 0) {
      obj.priority = pushPriorityToJSON(message.priority);
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    if (message.data) {
      const entries = globalThis.Object.entries(message.data) as [string, string][];
      if (entries.length > 0) {
        obj.data = {};
        entries.forEach(([k, v]) => {
          obj.data[k] = v;
        });
      }
    }
    if (message.sentAt !== 0) {
      obj.sentAt = Math.round(message.sentAt);
    }
    return obj;
  },

  create(base?: DeepPartial<WsPush>): WsPush {
    return WsPush.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsPush>): WsPush {
    const message = createBaseWsPush();
    message.pushId = object.pushId ?? 0;
    message.messageId = object.messageId ?? 0;
    message.category = object.category ?? 0;
    message.title = object.title ?? "";
    message.body = object.body ?? "";
    message.imageUrl = object.imageUrl ?? "";
    message.deepLink = object.deepLink ?? "";
    message.priority = object.priority ?? 0;
    message.ttlSeconds = object.ttlSeconds ?? 0;
    message.data = (globalThis.Object.entries(object.data ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.sentAt = object.sentAt ?? 0;
    return message;
  },
};

function createBaseWsPush_DataEntry(): WsPush_DataEntry {
  return { key: "", value: "" };
}

export const WsPush_DataEntry: MessageFns<WsPush_DataEntry> = {
  encode(message: WsPush_DataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsPush_DataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsPush_DataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsPush_DataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: WsPush_DataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<WsPush_DataEntry>): WsPush_DataEntry {
    return WsPush_DataEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsPush_DataEntry>): WsPush_DataEntry {
    const message = createBaseWsPush_DataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseWsPushAck(): WsPushAck {
  return { pushId: 0, messageId: 0, ackedAt: 0 };
}

export const WsPushAck: MessageFns<WsPushAck> = {
  encode(message: WsPushAck, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pushId !== 0) {
      writer.uint32(8).uint64(message.pushId);
    }
    if (message.messageId !== 0) {
      writer.uint32(16).uint64(message.messageId);
    }
    if (message.ackedAt !== 0) {
      writer.uint32(24).int64(message.ackedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsPushAck {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsPushAck();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.pushId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.messageId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.ackedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsPushAck {
    return {
      pushId: isSet(object.pushId)
        ? globalThis.Number(object.pushId)
        : isSet(object.push_id)
        ? globalThis.Number(object.push_id)
        : 0,
      messageId: isSet(object.messageId)
        ? globalThis.Number(object.messageId)
        : isSet(object.message_id)
        ? globalThis.Number(object.message_id)
        : 0,
      ackedAt: isSet(object.ackedAt)
        ? globalThis.Number(object.ackedAt)
        : isSet(object.acked_at)
        ? globalThis.Number(object.acked_at)
        : 0,
    };
  },

  toJSON(message: WsPushAck): unknown {
    const obj: any = {};
    if (message.pushId !== 0) {
      obj.pushId = Math.round(message.pushId);
    }
    if (message.messageId !== 0) {
      obj.messageId = Math.round(message.messageId);
    }
    if (message.ackedAt !== 0) {
      obj.ackedAt = Math.round(message.ackedAt);
    }
    return obj;
  },

  create(base?: DeepPartial<WsPushAck>): WsPushAck {
    return WsPushAck.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsPushAck>): WsPushAck {
    const message = createBaseWsPushAck();
    message.pushId = object.pushId ?? 0;
    message.messageId = object.messageId ?? 0;
    message.ackedAt = object.ackedAt ?? 0;
    return message;
  },
};

function createBaseWsPing(): WsPing {
  return { sentAt: 0 };
}

export const WsPing: MessageFns<WsPing> = {
  encode(message: WsPing, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sentAt !== 0) {
      writer.uint32(8).int64(message.sentAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsPing {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsPing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sentAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsPing {
    return {
      sentAt: isSet(object.sentAt)
        ? globalThis.Number(object.sentAt)
        : isSet(object.sent_at)
        ? globalThis.Number(object.sent_at)
        : 0,
    };
  },

  toJSON(message: WsPing): unknown {
    const obj: any = {};
    if (message.sentAt !== 0) {
      obj.sentAt = Math.round(message.sentAt);
    }
    return obj;
  },

  create(base?: DeepPartial<WsPing>): WsPing {
    return WsPing.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsPing>): WsPing {
    const message = createBaseWsPing();
    message.sentAt = object.sentAt ?? 0;
    return message;
  },
};

function createBaseWsPong(): WsPong {
  return { sentAt: 0 };
}

export const WsPong: MessageFns<WsPong> = {
  encode(message: WsPong, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sentAt !== 0) {
      writer.uint32(8).int64(message.sentAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsPong {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsPong();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sentAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsPong {
    return {
      sentAt: isSet(object.sentAt)
        ? globalThis.Number(object.sentAt)
        : isSet(object.sent_at)
        ? globalThis.Number(object.sent_at)
        : 0,
    };
  },

  toJSON(message: WsPong): unknown {
    const obj: any = {};
    if (message.sentAt !== 0) {
      obj.sentAt = Math.round(message.sentAt);
    }
    return obj;
  },

  create(base?: DeepPartial<WsPong>): WsPong {
    return WsPong.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsPong>): WsPong {
    const message = createBaseWsPong();
    message.sentAt = object.sentAt ?? 0;
    return message;
  },
};

function createBaseWsError(): WsError {
  return { code: "", message: "" };
}

export const WsError: MessageFns<WsError> = {
  encode(message: WsError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WsError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWsError();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WsError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: WsError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<WsError>): WsError {
    return WsError.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WsError>): WsError {
    const message = createBaseWsError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
