name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-smoke-test:
    runs-on:
      - iptv

    env:
      DB_HOST: 192.168.0.15
      DB_PORT: 3306
      DB_USER: root
      DB_PASSWORD: ju1io
      DB_NAME: iptv
      API_ADDR: 0.0.0.0:9065

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Download Go modules
        run: go mod download

      - name: Build Go binary
        run: |
          mkdir -p bin
          go build -v -o iptv .

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: web/yarn.lock

      - name: Install web dependencies
        working-directory: web
        run: yarn

      - name: Build Vue app
        working-directory: web
        run: yarn build

      - name: Ensure database exists
        run: |
          mysql \
            -h "$DB_HOST" \
            -P "$DB_PORT" \
            -u"$DB_USER" \
            -p"$DB_PASSWORD" \
            -e "CREATE DATABASE IF NOT EXISTS \`$DB_NAME\` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

      - name: Install backend and frontend artifacts on runner
        run: |
          mkdir -p /opt/iptv/web
          cp ./iptv /opt/iptv/iptv
          rm -rf /opt/iptv/web/dist
          cp -R ./web/dist /opt/iptv/web/dist
          chmod +x /opt/iptv/iptv

      - name: Write service environment file
        run: |
          mkdir -p /etc/iptv
          tee /etc/iptv/iptv.env >/dev/null <<EOF
          DB_HOST=${DB_HOST}
          DB_PORT=${DB_PORT}
          DB_USER=${DB_USER}
          DB_PASSWORD=${DB_PASSWORD}
          DB_NAME=${DB_NAME}
          API_ADDR=${API_ADDR}
          EOF
          chmod 600 /etc/iptv/iptv.env

      - name: Create or update user systemd service
        run: |
          mkdir -p "$HOME/.config/systemd/user"
          tee "$HOME/.config/systemd/user/iptv.service" >/dev/null <<EOF
          [Unit]
          Description=IPTV Go API Service
          After=network-online.target
          Wants=network-online.target

          [Service]
          Type=simple
          WorkingDirectory=/opt/iptv
          ExecStart=/opt/iptv/iptv
          EnvironmentFile=/etc/iptv/iptv.env
          Restart=always
          RestartSec=5

          [Install]
          WantedBy=default.target
          EOF

      - name: Restart and enable user systemd service
        run: |
          export XDG_RUNTIME_DIR="/run/user/$(id -u)"
          systemctl --user daemon-reload
          systemctl --user enable iptv.service
          systemctl --user restart iptv.service
          systemctl --user --no-pager --full status iptv.service
